{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window. Core defaults only; the app's own commands are the registry's (STD-004-TS §2 rule 3).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
