{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: exactly the commands in app/src/contract/registry.ts, and nothing from a default set (STD-004-TS §2 rule 3).",
  "windows": ["main"],
  "permissions": [
    "allow-workspace",
    "allow-views",
    "allow-element-detail",
    "allow-view-layout",
    "allow-file-text"
  ]
}
