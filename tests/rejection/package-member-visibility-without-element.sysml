// Rule: a MemberPrefix prefixes an element, it is not one.
// SysML 8.2.2.5.1 — PackageMember = MemberPrefix
//                     ( ownedRelatedElement += DefinitionElement
//                     | ownedRelatedElement = UsageElement )
// The element is not optional; only the visibility in front of it is.
public;
