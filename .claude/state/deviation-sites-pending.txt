# Register entries that depart from the specification (follow_xtext, follow_corpus,
# follow_spec_example) but have no PARSE-DEVIATION site in the parser yet, one per line
# as `NAME  # reason`. ADR-0022; scripts/check_deviation_sites.py fails an entry that is
# neither sited nor listed here, and one that is both. An entry leaves this list in the
# commit that implements what its deviation adds.
AnnotatingElement  # adds no text: under ADR-0015's resolution SysML's literal MetadataFeature reads SysML's PrefixMetadataMember and MetadataBody, and MetadataFeatureDeclaration prints MetadataUsageDeclaration's text; the deviation changes the element kind only, so SysML needs no site (adjudicated 2026-09-22)
BooleanExpression  # KerML's BooleanExpression is unimplemented
Expression  # KerML's Expression is unimplemented
ExpressionBody  # BodyExpression, the only route to it, is unimplemented
FlowDeclaration  # a KerML clause's missing '?'; FlowDeclaration@kerml is unimplemented, and SysML's UsageDeclaration is nullable, so SysML needs no site
ForVariableDeclarationMember  # ForLoopNode is unimplemented
FramedConcernUsage  # concern usages are unimplemented
Invariant  # KerML's Invariant is unimplemented
Step  # KerML's Step is unimplemented
