{
  "schema_version": 1,
  "generated": {
    "regenerated_utc": "2026-09-18T13:13:46Z",
    "gates": {
      "vendor": "pass",
      "namespace": "pass",
      "derived": "pass",
      "metaclass_map": "pass",
      "grammar_diff": "pass",
      "coverage": "pass",
      "corpus": "pass",
      "tests": "pass",
      "clippy": "pass",
      "doc": "pass",
      "deny": "pass"
    },
    "coverage": {
      "declared": 558,
      "implemented": 133,
      "unimplemented": 425,
      "absent": 0,
      "percent": 23.8
    },
    "tests": {
      "unit": 248,
      "snapshots": 8,
      "corpus_files": 311,
      "negative_cases": 77,
      "known_permissive": 0
    },
    "pins": {
      "omg_namespace": "20250201",
      "pilot_revision": "5cca16d846016e62bb1e54e0e50e675254a022ef",
      "pilot_bundle": "0.63.0",
      "vendor_pinned": 332,
      "vendor_total": 332
    },
    "commits": [
      "52d7f3b Read the two SysML usages written without a keyword",
      "925e861 Record where the parser is, and what the histogram does not tell you",
      "34d9b91 Read a feature written with no keyword at all",
      "91b7c82 Read KerML's Feature, in its keyword form",
      "a60aca9 Read PortDefinition, whose extra part consumes no tokens",
      "5fa2770 Rewrite CRATES.md as a map of this repository",
      "3b792f6 A binary per process shape, and every binary a shim (ADR-0018)",
      "3d0c583 Decide element identity: accept ADR-0016, supersede ADR-0009"
    ]
  },
  "authored": {
    "objective": "sv2-syntax parses a lossless CST against TWO grammars, chosen by file suffix: KerML and SysML have two start symbols, and sv2_syntax::parse requires a Language with no default (ADR-0014). It reads packages, imports, aliases, annotating elements as members, KerML's eight classifiers and its Feature in both forms, SysML's eight definitions plus PortDefinition, seven usages with all five feature specializations, the expression operator core, and both SysML usages written without a keyword: 133 of 558 productions. Diagnostics are typed: Parse::errors() returns Diagnostic, each with a TextRange, a namespaced code and a severity, and OffsetMap is the single designated offset boundary. The reference grammar is DERIVED IN FULL and FROZEN at sha256 0b9abe64f1f3ce2b: 554 of 554 live units verified in both languages. The RUST parser accepts 27 of 253 .sysml and 15 of 58 .kerml corpus files, recorded per file in tests/corpus-accepted.txt and ratcheted by scripts/corpus_sweep.py: the set may grow and must never shrink.",
    "next_step": "REQUIREMENT BEFORE ACTION. The body-shape question next_step asked has been answered, and it reorders the work.\n\nALL EIGHT specialised bodies share ONE shape, the same one DefinitionBody already has:\n    XBody = ';' | '{' XBodyItem* '}'\nwith exactly three deviations: StateDefBody takes a leading 'parallel'?, CaseBody a trailing ResultExpressionMember?, and CalculationBody a single CalculationBodyPart instead of Item*. So the BODIES are nearly free; the divergence is entirely in the ITEMS, and there it is not uniform:\n\n    RequirementBodyItem = DefinitionBodyItem | SubjectMember\n                        | RequirementConstraintMember | FramedConcernMember\n                        | RequirementVerificationMember | ActorMember\n                        | StakeholderMember\n    InterfaceBodyItem   = DefinitionBodyItem's six, with two substituted\n    ActionBodyItem      = NonBehaviorBodyItem\n                        | InitialNodeMember ActionTargetSuccessionMember*\n                        | SourceSuccessionMember? ActionBehaviorMember\n                          ActionTargetSuccessionMember*\n                        | GuardedSuccessionMember\n\nRequirementBodyItem is a SUPERSET of DefinitionBodyItem. That is the lever: requirement, concern and viewpoint definitions can reuse the body loop that already exists, with the six extra members unimplemented and reported like anything else. That is ~13 files for roughly the work PortDefinition took. Do it FIRST.\nActionBodyItem shares nothing with DefinitionBodyItem \u2014 it is the control-flow layer, with initial nodes, successions and guards. It is 37 files and genuinely the largest piece of work left; do it after, and expect it to be a body production rather than another table.\n\nAFTER THAT, re-measure. Counts are from 2026-09-18 after the keywordless usages:\nSysML, 224 failing: action 37, requirement 13, perform 10, metadata 9, then a tail.\nKerML, 43 failing: a long tail of threes and below. `feature` and `composite` are gone; what is left is FeatureElement's other nine alternatives (connector, step, succession), feature chains (`.`), multiplicity (`[`) and prefix metadata (`#`). No single KerML item is worth more than three files, so it is a grind rather than a lever.\n\nTWO LESSONS THIS SESSION KEPT RE-EARNING, both now cheap to avoid:\n(1) A first-error histogram says where to START, not what a change will BUY. `port` led fifteen files and bought one; `:>>` and `ref` led eighteen and bought two. Read the actual source lines before estimating \u2014 that is what turned `:>>` and `ref` from two unknown tokens into one known production pair.\n(2) Derive the table, do not choose it. Every table this session came from matching rule shapes across the verified units in the frozen grammar. Reading clause prose and typing a list would have put action and port on the definition spine, and both would have been wrong.\n\nDISPATCH ORDER IS A REAL HAZARD, newly learned. The keyword usages must be asked BEFORE ReferenceUsage, which must be asked before DefaultReferenceUsage, because `ref attribute y;` is an AttributeUsage whose prefix carries `ref` while `ref y;` is a ReferenceUsage. Getting it backwards dropped SysML acceptance 27 -> 24 and failed five existing tests. `at_member_element` and `membership` must ask in the SAME order or they disagree about what is here.\n\nTWO KNOWN GAPS, deliberate:\n(a) A bare `/* ... */` at member position is Comment's shortest form by 8.2.2.4.2 and is still attached as trivia. Lossless either way; the tree shape differs from the specification's. Switching it changes every tree with a comment in it and wants its own snapshot review. See Parser::at_annotating_member.\n(b) OffsetMap has line_col and no UTF-8/UTF-16 conversion, because ADR-0013 is still `proposed` and nothing consumes UTF-16 yet. When something does it goes in offset.rs and nowhere else (CRATES.md Seam 3).\n\nTHE WORKSPACE: seven crates, two binaries, ADR-0018. sv2-studio and sv2-wasm are stubs. Adding a crate means a row in BOTH deny.toml and STD-002-RS \u00a713.5, which check_standards_config.py diffs byte for byte. docs/CRATES.md is the map and has no authority; where it and a check disagree, the check is right.",
    "pending_decisions": [
      {
        "id": "crate-split",
        "question": "Boundary between sv2-hir and sv2-resolve is provisional. Revisit once implied specialization injection exists and its real dependencies are visible.",
        "blocks": []
      },
      {
        "id": "wiki-reproducibility",
        "question": "RESOLVED as far as it can be on one machine. The wikis are pinned by receipt: 1651 atoms in .claude/state/wiki-receipts.json, each with its clause, page, the wiki's region_sha256 to the source PDF span, and a file hash. Drift now fails the gate, and a citation naming a receipt that was never pinned now fails scripts/check_citations.py. NOT resolved: the wikis are not in the repository, because their 1651 atoms carry ~2.3 MB of verbatim OMG specification prose with no licence notice, and this repository is MIT and public. So a fresh clone can verify nothing until it builds its own wikis from the specification PDFs, which are not redistributable either. Revisit only if OMG's licensing changes, or if the wiki builder can be pinned and rerun from locally-held PDFs as part of a bootstrap.",
        "blocks": [
          "verifying any clause citation on a machine that has not built the wikis"
        ]
      },
      {
        "id": "state-commits-fixed-point",
        "question": "RESOLVED. regen_state.py recorded `git log --oneline -8` in the generated block and --check compared it, which has no fixed point: the commit carrying state.json cannot record its own hash, and amending only moves it. The gate was therefore red after every commit, for ever. Fixed by naming the compared measurements explicitly \u2014 COMPARED = (coverage, tests, pins) \u2014 and a pure is_stale() over them, with four tests including the one that says a new commit is not staleness. `commits` is still measured and written for whoever reads the block at session start; it is simply not evidence. Nothing else read it: not state_report.py, not the schema's required list. NOT weakened: coverage, tests and pins are still compared exactly as before, so an un-regenerated block after real work still fails.",
        "blocks": []
      },
      {
        "id": "expression-layer-approach",
        "question": "RESOLVED: the derive-grammar six-phase pipeline. It runs, and the expression layer is being derived through it starting from KerML 8.2.5.8.1. Two pipeline defects had to be fixed first (plan enumerated the Xtext inventory; freeze check went red on the first derived unit); both are fixed and tested.",
        "blocks": []
      },
      {
        "id": "derivation-evidence-in-repo",
        "question": "RESOLVED. A unit no longer embeds its clause: it keeps the citation and a sha256 of the clause text, and grammar_next.py joins the text from the local export at read time. The units are therefore committed, so the derived rules, decisions and evidence survive a clone and are reviewable in a diff, while no specification prose enters the repository. check_derivation_text.py holds the line on every gate. NOT resolved by this: a fresh clone still cannot re-derive or verify a clause without building the wikis from locally-held PDFs, which is the standing wiki-reproducibility limit.",
        "blocks": []
      },
      {
        "id": "ADR-0016",
        "question": "Proposed: stable element identity via petname IDs carried in inline textual notes. Would answer ADR-0009; both are open in decisions.json.",
        "blocks": [
          "stable element handles in sv2-resolve"
        ],
        "adr": "docs/adr/0016-element-identity-via-petname-notes.md"
      },
      {
        "id": "ADR-0017",
        "question": "Proposed: view-scoped JSON Lines sidecar for diagram layout and styling.",
        "blocks": [
          "sidecar schema"
        ],
        "adr": "docs/adr/0017-view-scoped-json-lines-sidecar-for-diagram-layout-and-styling.md"
      },
      {
        "id": "bracket-role",
        "question": "How is '[' disambiguated between MultiplicityPart and BracketExpression? Both are implemented-or-planned in the same grammar and the token is identical. The working hypothesis is positional -- a '[' reached from UsageDeclaration's FeatureSpecializationPart is a multiplicity, a '[' reached from an expression operand is a BracketExpression -- which is what the current code already does by construction, since multiplicity_part is only called from feature_specialization_part. Confirm that holds for every corpus shape before BracketExpression lands, in particular where a value part's expression is followed by a unit: `attribute m = 1200 [kg];` reaches the '[' from the expression side with a MultiplicityPart already consumed on the declaration side.",
        "blocks": [
          "BracketExpression, IndexExpression, and the 297 corpus files that write a bracketed unit"
        ]
      },
      {
        "id": "max-depth",
        "question": "Is MAX_DEPTH = 400 in crates/sv2-syntax/src/parser.rs the right recursion limit, and should it be configurable? It was introduced this session because the recursive expression parser OVERFLOWED THE STACK, which aborts the process and so is strictly worse than the panic invariant 3 forbids. Measured thresholds before the guard: unary operators between 2000 and 5000 levels, parenthesised expressions and nested bodies between 5000 and 10000. Note the nested-body path had this flaw ALREADY -- it is not new with the expression layer. 400 is far past anything a person writes, but it is a number nobody derived from a source, and it changes what the parser accepts.",
        "blocks": [
          "nothing today; revisit if a generated model is ever rejected for depth"
        ]
      }
    ],
    "log": [
      {
        "summary": "Closed the SysML grammar and froze the reference. 473 to 554 of 554 live units verified, none pending and none in conflict; frozen at sha256 0b9abe64f1f3ce2b, 554 productions, ledger entry 1. The oracle went from 155 to all 311 corpus files accepted, still catching all 37 negatives with none leaked. Four filed specification defects repaired and recorded, each issue page fetched and read: SYSML21-408 and -624 (both in SendNode's single line), -400 (ControlNodePrefix) and -401 (TriggerExpression). ForVariableDeclarationMember adjudicated, which left SysML with no unreachable production. The oracle's state guard was re-derived from a measurement: ~143 items per token scaling linearly, so the 400_000 absolute cap was mis-calibrated rather than the grammar ambiguous, and it is now a per-token budget with four tests. Also declared sv2-ast, sv2-hir and sv2-resolve as stub crates, and proposed ADR-0016 and ADR-0017.",
        "date": "2026-09-17",
        "gate": "green"
      },
      {
        "date": "2026-09-17",
        "gate": "green",
        "summary": "Implemented the expression operator core: OwnedExpression by precedence climbing over the fifteen tiers of KerML 8.2.5.8.1 table 6, plus MultiplicityPart, ValuePart and ElementFilterMember, which it unblocked. Coverage 57 to 106 of 558; negative cases 49 to 65. Precedence is NOT in the grammar and cannot be -- note 2 of that clause says so -- so the parser reads docs/operator-precedence.toml, and a test diffs that file against the parser's INFIX table both ways so neither can be edited alone; deviations.json had already recorded the decision under BinaryOperatorExpression as spec_only/follow_spec. Found and fixed a defect this introduced: deep nesting overflowed the stack, which aborts rather than panics and so breaks invariant 3; added MAX_DEPTH and a round-trip test at 50000 levels for parentheses, unary operators and nested bodies. spec-conformance-reviewer returned one blocking finding, all fixed: the FeatureSpecializationPart comment quoted the Pilot's '( -> FeatureSpecialization )+' syntactic predicate as though it were KerML 8.2.4.3.1, and that '->' is in neither specification's BNF. Also committed a draft docs/CRATES.md and the author's spelling corrections to README.md and NOTICE.md."
      }
    ]
  }
}
