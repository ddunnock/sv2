{
  "schema_version": 1,
  "generated": {
    "regenerated_utc": "2026-09-23T21:41:34Z",
    "gates": {
      "vendor": "pass",
      "namespace": "pass",
      "derived": "pass",
      "metaclass_map": "pass",
      "grammar_diff": "pass",
      "coverage": "pass",
      "corpus": "pass",
      "tests": "pass",
      "clippy": "pass",
      "doc": "pass",
      "deny": "pass"
    },
    "coverage": {
      "declared": 554,
      "implemented": 418,
      "unimplemented": 136,
      "absent": 0,
      "percent": 75.5
    },
    "tests": {
      "unit": 634,
      "snapshots": 8,
      "corpus_files": 311,
      "negative_cases": 255,
      "known_permissive": 0
    },
    "pins": {
      "omg_namespace": "20250201",
      "pilot_revision": "5cca16d846016e62bb1e54e0e50e675254a022ef",
      "pilot_bundle": "0.63.0",
      "vendor_pinned": 332,
      "vendor_total": 332
    },
    "commits": [
      "136bbdb Read KerML multiplicities, on features, classifiers and connector ends",
      "6eae9ee Stop rejecting KerML multiplicities by absence: the next commit reads them",
      "8fab0cc Close the session: the action-node layer whole and every .sysml file read, KerML multiplicity next",
      "51a5570 Read invocations through a feature chain, `a.b(x)`; every .sysml file parses",
      "c424282 Fold reserved-words-per-language into keyword-table-per-language",
      "7c6166e Read for loops, completing the action nodes",
      "f1eb89d Read if actions, `if c { ... } else if d { ... } else { ... }`",
      "4d3bd43 Stop rejecting `if i < 0 { }` by absence: the next commit reads it"
    ]
  },
  "authored": {
    "objective": "sv2-syntax parses a lossless CST against TWO grammars, chosen by file suffix: KerML and SysML have two start symbols, and sv2_syntax::parse requires a Language with no default (ADR-0014). It reads packages, imports, aliases, FLOW USAGES (SysML 8.2.2.16: both FlowDeclaration alternatives, the `of` payload in all three PayloadFeature forms, and flow ends of one, two or more segments), annotating elements as members, KerML's eight classifiers and its Feature in both forms, SysML's eight definitions plus PortDefinition, seven usages with all five feature specializations, and both keywordless usages. It reads the DECLARATION LAYER of requirements, constraints, calculations and actions: RequirementDefinition, ConstraintDefinition, CalculationDefinition, ActionDefinition and their three specialised bodies (RequirementBody, CalculationBody, ActionBody), SubjectMember, RequirementConstraintMember, ReturnParameterMember, ActionUsage and PerformActionUsage -- and the second alternative of ActionBodyItem, the first of the CONTROL-FLOW LAYER, whole: InitialNodeMember (`first X;`) and the ActionTargetSuccessionMember* after it in its TargetSuccession form (`then X;`, with a SourceEnd and a ConnectorEnd over OwnedReferenceSubsetting), and the THIRD over the behaviour usages it has: SourceSuccessionMember (`then action a;`, and `then part p;` in the two other item productions that admit it) and the `then X;` targets after a BehaviorUsageMember -- and the CONTROL NODES, ControlNode whole (MergeNode, DecisionNode, JoinNode, ForkNode, SysML 8.2.2.17.3) owned through ActionNodeMember, ActionBehaviorMember's second alternative, with the same `then` prefix and target loop, in action and calculation bodies only. The target loop reads ALL THREE of ActionTargetSuccession's alternatives, so the production itself is marked: TargetSuccession, GuardedTargetSuccession (`if <expr> then X;`, SysML 8.2.2.17.8) over GuardExpressionMember, TransitionSuccessionMember, TransitionSuccession and EmptyEndMember -- a TransitionUsage, not a SuccessionAsUsage, told from IfNode and from a ConditionalExpression result by the `then` its guard must reach, and DefaultTargetSuccession (`else X;`), told on its one reserved keyword. ALL FOUR ActionBodyItem ALTERNATIVES ARE NOW READ over the elements this parser has: the fourth is GuardedSuccessionMember over GuardedSuccession (`first a if x then b;`, with the optional `succession` declaration), the only succession that writes its own source and the only alternative that takes no target-succession suffix, over FeatureChainMember@sysml and OwnedFeatureChainMember@sysml (8.2.2.17.5). The SUCCESSION PAIR is read in both languages: SuccessionAsUsage@sysml (8.2.2.13.3, `first a then b;` and `succession s first a then b;`, a NonOccurrenceUsageElement over UsagePrefix) and Succession@kerml with SuccessionDeclaration@kerml (8.2.5.5.3, `succession a then b;` and its FeatureDeclaration form), both over ConnectorEndMember. Its SysML sibling BindingConnectorAsUsage@sysml (8.2.2.13.2, `bind a = b;` and `binding b : T bind a = b;`) is read too, and so is its KerML counterpart, BindingConnector@kerml with BindingConnectorDeclaration@kerml (8.2.5.5.2, `binding a = b;`, `binding ab of a = b;`, and the empty `binding { end feature ... }`). AssertConstraintUsage@sysml (8.2.2.20) is read in both alternatives -- `assert not? constraint <decl> <body>` and `assert not? <reference> <spec>? <body>` -- as a BehaviorUsageElement, so a `then` may precede it and target successions follow it in an action body; ConstraintUsage@sysml, the unasserted `constraint c : C { ... }` (8.2.2.20), is read too, told from ConstraintDefinition by the `def`, and so is RequirementUsage@sysml (8.2.2.21.2, `requirement <'1.1'> r : R { subject ...; }`), nested requirements included. The ENUMERATION DEFINITION is read (8.2.2.8): `enum def E :> A { doc /* */ enum a = 1; b { } = 60.0; }`, its body admitting enumerated values and annotations only, through EnumerationBody, EnumerationUsageMember, EnumeratedValue and AnnotatingMember; EnumerationDefinition and EnumeratedValue are read whole, prefix metadata included (the latter by the sited deviation EnumeratedValue), and marked. CalculationUsage@sysml (8.2.2.19, `calc c : C { ... }`) is read, told from CalculationDefinition by the `def` as ActionUsage is from ActionDefinition, over ActionUsageDeclaration and CalculationBody. VARIANTS are read (VariantUsageMember and VariantReference, 8.2.2.6.1/.3, in every definition, usage, requirement, action, calculation and state body); the DEPENDENCY in both grammars (SysML 8.2.2.3 with its DependencyDeclaration, KerML 8.2.3.2 inline), and KerML's own RelationshipBody (8.2.3.1), which admits owned related elements, so relationship_body dispatches on the language; the CONJUGATED PORT TYPING `: ~P` (8.2.2.12), SysML only; and THE STATE LAYER (8.2.2.18) with Body::State: state definitions and usages, `parallel`, transitions and target transitions, the accepter with `via`, the `at`/`after`/`when` triggers, entry/do/exit actions, entry transitions, `do` effects and both `exhibit` forms. Only the send and assignment action nodes and the `new` ConstructorExpression are missing from it, and a send or assign written as a state action or effect is reported as not implemented. The expression layer reads the fifteen precedence tiers, the postfix pair FeatureChainExpression and BracketExpression, and now the INVOCATION -- InvocationExpression over ArgumentList in both its positional and named forms -- and the reference layer reads OwnedFeatureChain. 342 of 554 GRAMMAR UNITS (61.7%), usages owned through the four usage memberships of 8.2.2.6.1 rather than DefinitionMember -- counted in ADR-0015's live units, where a production the two languages state differently is two units, not in the inventory's 558 names. Diagnostics are typed: Parse::errors() returns Diagnostic, each with a TextRange, a namespaced code and a severity, and OffsetMap is the single designated offset boundary. The reference grammar is DERIVED IN FULL and FROZEN at sha256 0b9abe64f1f3ce2b: 554 of 554 live units verified in both languages. The RUST parser accepts 180 of 253 .sysml and 19 of 58 .kerml corpus files, recorded per file in tests/corpus-accepted.txt and ratcheted by scripts/corpus_sweep.py: the set may grow and must never shrink. It also reads the ACTION NODES that send, accept and assign (SysML 8.2.2.17.4-5: SendNode with SenderReceiverPart, AcceptNode, AssignmentNode with its AssignmentTarget decomposition), their state and transition forms (8.2.2.18.1, 8.2.2.18.3, completing StateActionUsage and EffectBehaviorUsage), and KerML's ConstructorExpression (`new T(...)`, 8.2.5.8.3, shared). And CONNECTION USAGES (SysML 8.2.2.13.1: ConnectionUsage with its binary and n-ary ConnectorParts, and ConnectorEnd's cross multiplicity in SysML) and END FEATURES (8.2.2.6.2-3: EndUsagePrefix with its OwnedCrossFeature, on occurrence usages by deviation OccurrenceUsagePrefix, on UsagePrefix usages and on ReferenceUsage). It REPORTS ITS OWN DEPARTURES from the specification (ADR-0022): text admitted only by a recorded deviation carries an Info PARSE-DEVIATION diagnostic naming the register entry, kept apart from errors in Parse::deviations(), and `sv2 parse --strict` / Parse::is_spec_conformant() treat those as failures. And the CASE LAYER whole (SysML 8.2.2.22-25: CaseDefinition/Usage, AnalysisCase*, VerificationCase*, UseCase* over one CASES table of keyword runs, CaseBody as Body::Case, ObjectiveMember, ActorMember in requirement and case bodies, RequirementVerificationMember, IncludeUseCaseUsage), and the METADATA LAYER whole (8.2.2.27: MetadataDefinition, MetadataUsage as AnnotatingElement's fourth alternative wherever an annotating element stands, MetadataUsageDeclaration, MetadataBody with its own item loop, MetadataBodyUsage) with the `#` PREFIX: PrefixMetadataMember/Usage, PrefixMetadataAnnotation on Dependency@sysml, both extension keywords in every prefix and inline site in the grammar's order, ExtendedDefinition and ExtendedUsage, SysML only. And INTERFACES (8.2.2.14: InterfaceDefinition, InterfaceBody as Body::Interface with DefaultInterfaceEnd, InterfaceUsage over both declaration alternatives, InterfaceEnd sharing ConnectorEnd's reader less its trailing-multiplicity deviation) and LIBRARY PACKAGES in both languages (8.2.2.5.1, KerML 8.2.5.13, `standard` optional by the sited deviation LibraryPackage). And EVENT OCCURRENCES (8.2.2.9.2: EventOccurrenceUsage over both alternatives, `event a.b[1] :>> e;` by reference and `event occurrence e;` by declaration) and the KEYWORDLESS OCCURRENCES (8.2.2.9.1-2: IndividualDefinition, `individual def X;` with its EmptyMultiplicityMember last as the specification writes it; IndividualUsage and PortionUsage, `individual x;` and `snapshot s;`, told from a keyword usage's OccurrenceUsagePrefix by nth_opens_usage), all StructureUsageElements, read wherever an occurrence usage is. And MESSAGES (8.2.2.16: Message over MessageDeclaration's two alternatives, events as MessageEventMember/MessageEvent over OwnedReferenceSubsetting, the payload through flow_payload_feature_member), SATISFY (8.2.2.21.2: SatisfyRequirementUsage over both alternatives and its four Satisfaction* subject productions; deviation SatisfyRequirementUsage SITED), ALLOCATIONS (8.2.2.15: AllocationUsage over AllocationUsageDeclaration, ConnectionUsage's shape with no ValuePart), and CONCERNS (8.2.2.21.3: ConcernDefinition, ConcernUsage; 8.2.2.21.1: StakeholderMember, FramedConcernMember over both FramedConcernUsage alternatives, deviation FramedConcernUsage SITED with a depth-tracked note on requirement-only members of a framed reference body), so all six of RequirementBodyItem's own members are read. Every `FeatureSpecializationPart?` goes through optional_feature_specialization_part. The POSTFIX EXPRESSION LAYER of KerML 8.2.5.8.2 is whole: FeatureChainExpression, BracketExpression, IndexExpression (`x#(i)`), FunctionOperationExpression (`x->f(...)`, `x->reduce '+'`, `x->forAll {in i; ...}`), SelectExpression (`x.?{...}`) and CollectExpression (`x.{...}`), with BodyArgumentMember, the function-reference argument chain, and BodyExpression/ExpressionBodyMember (8.2.5.8.3), also as a BaseExpression; SysML reads ExpressionBody as CalculationBody by the sited deviation ExpressionBody, braced form only (decision expression-body-semicolon, settled), and a .kerml body is reported at its `{` (FunctionBodyPart@kerml is unimplemented). usage_completion_follows asks opens_body_expression so a body's `{` is not a usage completion. And the VIEW LAYER (8.2.2.26): ViewpointDefinition and ViewpointUsage (requirements by the keyword `viewpoint`), ViewDefinition with ViewDefinitionBody (Body::ViewDefinition), ViewUsage with ViewBody (Body::View), ViewRenderingMember and ViewRenderingUsage over both alternatives, and Expose, MembershipExpose and NamespaceExpose; and FILTERED IMPORTS in both languages (FilterPackage@sysml/@kerml, FilterPackageImport@sysml, FilterPackageMember), which completes NamespaceImport; `imported` builds a whole MembershipImport or NamespaceImport for both import_declaration and expose. And, from the eleventh close, SUCCESSION FLOWS (SuccessionFlowUsage, 8.2.2.16, reading FlowDeclaration by flow_usage's method) and the WHOLE ACTION-NODE LAYER (ActionNode marked, all eight alternatives): TerminateNode (8.2.2.17.6) and the structured nodes of 8.2.2.17.7, WhileLoopNode (`while`/`loop`, with `until`), IfNode (with `else` and `else if` through IfNodeParameterMember, told from GuardedTargetSuccession and ConditionalExpression by if_node_body_follows) and ForLoopNode (ForVariableDeclarationMember read by deviation, follow_xtext, which adds no text), over the shared ActionNodePrefix, ExpressionParameterMember and ActionBodyParameter(Member); action_node_keyword_index_at finds each node's keyword past its declaration, and the state and transition forms ask it for their three keywords only. InstantiatedTypeMember is read whole, its feature-chain alternative (`a.b(x)`) in both languages through instantiated_type_member, for invocations, `->` operations and constructors. Every pinned .sysml file parses.",
    "next_step": "NEWEST, 2026-09-23 (after the eleventh close) -- read this first. KerML MULTIPLICITY IS DONE: 6eae9ee retired its absence cases, 136bbdb read OwnedMultiplicity@kerml, OwnedMultiplicityRange@kerml (new node kind) and MultiplicityBounds@kerml, the classifier's `[1]`, and ConnectorEnd's leading cross multiplicity in both languages (ConnectorEnd, OwnedCrossMultiplicityMember, OwnedCrossMultiplicity marked). Gate green, 418 of 554 units, 253/253 .sysml, 20/58 .kerml (unchanged: the classifier files also write `unions`/`conjugates`), 255 rejections, pushed. NEXT: Connector@kerml with ConnectorDeclaration, BinaryConnectorDeclaration and NaryConnectorDeclaration (KerML 8.2.5.5.1; look each up with jq on bnf-productions.json), the first error in 7 .kerml files (examples/Simple Tests/Connectors.kerml:7 `connector c1 from a to b;`, KerML Spec Annex A Examples/A-3-3-OneToOneConnectors.kerml:13 `connector fixWheel : BikeWheelFixed from [1] rollsOn to [1] holdsWheel;`, FeatureChains.kerml:18 `connector f.a to a.g;`, VehicleTanks.kerml:27 `connector eng to tanks.main1;`); ConnectorEndMember and ConnectorEnd are already read and shared. Then the TypeRelationshipPart family on classifiers (`unions`, `conjugates`: A-3-5-TimingForStructures.kerml:27, Conjugation.kerml:6, with their absence files conjugation-part-is-not-implemented.kerml and type-relationship-part-is-not-implemented.kerml), then SuccessionFlow@kerml. The eleventh close's paragraph below is otherwise current.\n\n\nNEWEST, 2026-09-23 (eleventh close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 412 of 554 units, ALL 253 of 253 .sysml and 20 of 58 .kerml accepted, 253 rejection files caught, last code commit 51a5570, everything pushed to origin/main. Deviation sites: 15 sited, 7 pending (ForVariableDeclarationMember now pending with the reason 'adds no text').\n\nDONE SINCE THE TENTH CLOSE (fourteen commits, each code commit through a spec-conformance review whose findings were fixed before commit, each new rejection case proved by planted mutations): a684449 + daf5709 SuccessionFlowUsage (+1 unit, +3 files); a0ea0ef + 4c71e51 TerminateNode (+1, +3); 80415c1 WhileLoopNode with ActionNodePrefix, ExpressionParameterMember, ActionBodyParameterMember and ActionBodyParameter (+5, +1); 4d3bd43 + f1eb89d IfNode and IfNodeParameterMember (+2, +1; if_node_body_follows tells IfNode from GuardedTargetSuccession and a calculation's ConditionalExpression); 7c6166e ForLoopNode, ForVariableDeclaration(Member), and ActionNode marked (+4, +3; the control-flow absence file retired whole); 51a5570 InstantiatedTypeMember's feature-chain alternative, `a.b(x)`, in both languages (+3 incl. OwnedFeatureChainMember@kerml and FeatureChain@kerml, +2: both Annex A copies). c424282 folded a duplicate pending decision into keyword-table-per-language. THREE SPEC EXAMPLES CONFLICT WITH THE GRAMMAR and are recorded, not patched: state-body-terminate (7.18.3 OnOff6), if-then-brace-example (7.17.11's third), and keyword-table-per-language now carries 7.17.12's `action step` (44 KerML-only words refused as SysML names). They want one grammar-adjudicator pass. Lessons: (1) a recogniser can mask a reader AND a reader a recogniser -- plant the mutant in both when one alone is equivalent; (2) the corpus sweep's declared-reason check catches mutants the unit tests miss, so a rejection file's `rejects:` line must name the exact line the diagnostic lands on (a missing `;` reports at the NEXT token); (3) cargo fmt reflows long literals, so a scripted edit must anchor on formatted text -- write edit scripts to files and check they applied; (4) an expectation must come from the production: `a..b(1)` is a range, not a malformed chain.\n\nNEXT: KerML multiplicity, OwnedMultiplicity@kerml = OwnedMultiplicityRange = MultiplicityBounds (KerML 8.2.4.x, `'[' MultiplicityBound ( '..' MultiplicityBound )? ']'`, look the clause up with jq on bnf-productions.json before citing), and the TypeDeclaration/ClassifierDeclaration slot that takes it. The .kerml sweep's first errors at close: 7 `unexpected connector` (examples/Simple Tests/Connectors.kerml:7 `connector c1 from a to b;`, KerML Spec Annex A Examples/A-3-3-OneToOneConnectors.kerml:13 `connector fixWheel : BikeWheelFixed from [1] rollsOn to [1] holdsWheel;`), 6 `expected ; or { after a classifier declaration` (A-2-ModelingInstances.kerml:8 `classifier MyBike [1] specializes Bicycle;`, JohnIndividualExample.kerml:52 `class all JohnLife[0..1] specializes John, Occurrences::Life;`, plus `unions` and `conjugates` relationship parts), 4 `unexpected step`, 3 `portion`, 3 `end`. Multiplicity first because it is under both the classifier failures and the connector ends' `[1]` (OwnedCrossMultiplicity, which also retires tests/rejection/kerml-connector-end-cross-multiplicity-is-not-implemented.kerml); then Connector@kerml with ConnectorDeclaration, BinaryConnectorDeclaration and NaryConnectorDeclaration (KerML 8.2.5.5.1); then SuccessionFlow@kerml, whose absence file is tests/rejection/kerml-succession-flow-is-not-implemented.kerml. Re-measure before believing any count: blank the construct in scratch copies and re-sweep. Also open: whether BehaviorUsageElement, StructureUsageElement and OccurrenceUsageElement (SysML 8.2.2.6.4) can now be marked, every alternative appearing to be read -- check each alternative against usage_element_of_class before marking.\n\n\nNEWEST, 2026-09-23 (tenth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 395 of 554 units, 240 of 253 .sysml and 20 of 58 .kerml accepted, 239 rejection files caught, last code commit 72ef125, everything pushed to origin/main. Deviation sites: 15 sited, 7 pending.\n\nDONE SINCE THE NINTH CLOSE (sixteen commits, each through a spec-conformance review whose findings were fixed before commit, and each new rejection case proved by planted mutations): 03c8416 + 5649eb2 FunctionOperationExpression and BodyExpression (+12 units, +2 files; deviation ExpressionBody sited; pending expression-body-semicolon added); e663c21 + ff4e6cf IndexExpression (+1 unit, +8 .sysml +1 .kerml); 22a1f5b + b733786 + 10d5ae9 SelectExpression and CollectExpression (+2 units, +2 files; IndexExpression, SelectExpression and CollectExpression deviation entries gained BracketExpression's 'naming difference, not a gap' evidence); b2356c1 viewpoints (+2, +1 file); ac6155b views and render (+6; requirement_body_member and structure_usage_element split out for clippy); 6470178 FilterPackage (+6 incl. MembershipImport and NamespaceImport, +2 files); f612d52 + 72ef125 Expose (+3, +4 files). Lessons: (1) restoring files from a tar keeps their old mtimes, so cargo skips the rebuild and the sweep reports a stale binary's answers -- touch restored files; (2) a mutant that is still rejected for the wrong reason survives a parse_rejected test -- assert the construct's node is NOT begun (`tanks#M`, `private expose`); (3) count corpus line numbers with grep -n, never from sed output by eye (two off-by-one citations this session); (4) the pinned BNF's `// Clause` headers are the authority for a clause number (ViewUsage is 8.2.2.26.2, not .1).\n\nNEXT: SuccessionFlowUsage (SysML 8.2.2.16, SysML-textual-bnf.kebnf:845, `OccurrenceUsagePrefix 'succession' 'flow' FlowDeclaration DefinitionBody`), the first error in all three remaining `succession` files: examples/Simple Tests/PartTest.sysml:27 `succession flow x.p to a1.aa.receiver;`, training/14. Action Definitions/Action Succession Example-2.sysml:17 `succession flow from focus.image to shoot.image;`, examples/Flashlight Example/Flashlight Example.sysml:48. FIRST move the placeholder: text_no_implemented_production_accepts_becomes_an_error_node and unimplemented_definition_body_items_are_reported_at_the_body (crates/sv2-syntax/tests/parser.rs) hold a succession flow as their unreadable example since f612d52; pick one that lands only with its own production, as a retirement commit. FlowUsage already reads FlowDeclaration (see flow_usage and at_flow_usage); at_succession_as_usage deliberately refuses `succession flow`, and SuccessionFlowUsage is a StructureUsageElement, so it joins structure_usage_element. AFTER THAT, by first error: the action control nodes (8.2.2.17.x) -- TerminateNode (`terminate`, 2 files, plus `then terminate;` in training/19. Terminate Actions/Terminate Actions Example-1.sysml:19), WhileLoopNode (`loop`, 2), ForLoopNode (`for`, 2, where ForVariableDeclarationMember is a pending deviation site) and IfNode (1); and Annex A SimpleVehicleModel.sysml:1232 `vehicleSpecification.vehicleMassRequirement(vehicle_uut)`, an invocation through a feature chain, which is InstantiatedTypeMember's unimplemented OwnedFeatureChainMember alternative (see invocation_expression).\n\nNEWEST, 2026-09-23 (ninth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 363 of 554 units, 221 of 253 .sysml and 19 of 58 .kerml accepted, 235 rejection files caught, last code commit 893dce4, everything pushed to origin/main. Deviation sites: 14 sited, 8 pending.\n\nDONE SINCE THE EIGHTH CLOSE (seven commits, each through a spec-conformance review whose findings were fixed before commit): 4d3e06e Message (+4 units, +10 files; advisories: a truncated quote, and a claim that the Pilot's PayloadFeatureMember accepts the spec's text -- it does not, its Payload has a fourth `Identification? ValuePart` alternative, settled by deviation Payload); 2d3c97c + 789e8bd SatisfyRequirementUsage (+5 units, +5 files; deviation SatisfyRequirementUsage sited: only `assert not satisfy` is the clause's own; the two tests that used `satisfy` as their unimplemented example now use a view usage with `expose`); 38c8529 AllocationUsage (+2 units, +5 files; pending decision allocation-arity added for 7.15.2's 'always binary' against ConnectorPart's n-ary form); fa3f315 + 5e3d072 concerns (+6 units, +0 files; review BLOCKING: the stakeholder implied specialization cited no constraint, it is PartUsage::checkPartUsageStakeholderSpecialization 8.3.11.3); 893dce4 optional_feature_specialization_part (+1 file; settles feature-specialization-multiplicity-gap). Lessons: (1) `--record` records the LAST sweep, and the last sweep of a mutation loop is a mutant -- re-sweep the clean tree before recording; (2) a rejection file can be rejected for the wrong reason by an innocent-looking reserved word (`send` in `driver.send`) -- check the diagnostic's column, not just its code; (3) tests written from memory of the grammar were wrong three times (`message m to b;` is valid, `entry perform x;` is not, a corpus line cited for a constructed case) -- read the production before writing the case; (4) clippy's 60-line test limit and 15-point cognitive complexity bite on nearly every layer: split tests by alternative, dispatchers by family.\n\nNEXT: FunctionOperationExpression (KerML 8.2.5.8.2, KerML-textual-bnf.kebnf:1134), the `->` invocation, now the first error in 6 of the 32 rejected .sysml files (examples/Analysis Examples/Vehicle Analysis Demo.sysml:206 `(1..sc.n-1)->forAll {in i: Integer; ...}`, validation/07-Variant Configuration/7b-Variant Configurations.sysml:127 `rearWheelChoice->forAll {in ref w; w == ...}`, plus Geometry Examples/VehicleGeometryAndCoordinateFrames, v1 Spec Examples/D.4.7.8 Dynamics/HSUVDynamics, training/33. Analysis/Analysis Case Definition Example, validation/15-Properties-Values-Expressions/15_05): `PrimaryArgumentMember '->' InstantiatedTypeMember ( BodyArgumentMember | FunctionReferenceArgumentMember | ArgumentList ) EmptyResultMember`. The corpus writes the BodyArgumentMember form, which reaches BodyExpression and ExpressionBody -- ExpressionBody is a PENDING deviation site ('BodyExpression, the only route to it, is unimplemented'), and in SysML it reads CalculationBody (see primary_expression's comment at the NOT-implemented list, which also names the rejection cases to retire, e.g. `x->size()`). This is the expression layer: derive it through the derive-grammar pipeline as the expression units were, check PrimaryExpression's left-recursion shape in the existing primary_expression before adding a postfix operator, retire the absence rejections first, and site deviation ExpressionBody when BodyExpression lands. AFTER THAT, by the same first-error measure (2026-09-23): IndexExpression `#(` 4 (KerML 8.2.5.8.2), viewpoint 3 and view 2 (the view layer, 8.2.2.26), `succession` 3 (SuccessionAsUsage's keyword form or SuccessionFlowUsage), then `terminate`, `loop`, `for` and `[@` at 2 each.\n\nNEWEST, 2026-09-23 (eighth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 346 of 554 units, 200 of 253 .sysml and 19 of 58 .kerml accepted, 224 rejection files caught, last code commit 468bea3, everything pushed to origin/main.\n\nDONE SINCE THE SEVENTH CLOSE (two commits, each through a spec-conformance review whose findings were fixed before commit): c92f41f EventOccurrenceUsage (+1 unit, +7 files; review clean); 468bea3 IndividualDefinition, IndividualUsage, PortionUsage (+3 units, +13 files; review caught OccurrenceDefinition cited as 8.3.9.1, it is 8.3.9.3 receipt 69f220d7, and a quote of 7.9.4 that silently dropped the source's doubled 'the the', now quoted with [sic]). nth_opens_usage was extracted from at_extended_usage and is the shared test for 'a Usage opens here'. definition_element hit clippy's cognitive-complexity limit (16/15); simple_definition_element was split out, as behavior_usage_element was. Lessons: (1) a rule about what may NOT follow a token needs a case where the forbidden thing would otherwise fit -- `event request m.source;` failed at the `.` for an unrelated reason, so the no-name mutation survived until `event m.source request;` was added; (2) an equivalent mutant is possible when dispatch order already guards a recogniser: removing nth_opens_usage from at_individual_or_portion_usage changes no accepted or rejected text today, and the reason is recorded at the recogniser rather than the check deleted.\n\nNEXT: Message@sysml (SysML 8.2.2.16, kebnf:821), now the first error in 10 of the 53 rejected .sysml files (e.g. training/27. Occurrences/Interaction Example-2.sysml:30 `message setSpeedMessage of SetSpeed;`, validation/17-Sequence Modeling/17a-Sequence-Modeling.sysml:26 `message publish_message of Publish[1] from producer.publish_source_event to server.publish_target_event;`, Simple Tests/ConnectionTest.sysml:59 `message : F from p to p;`, Interaction Realization-1.sysml:51 `message :>> setSpeedMessage = ...;`): `OccurrenceUsagePrefix 'message' MessageDeclaration DefinitionBody`, with MessageDeclaration = `UsageDeclaration ValuePart? ( 'of' FlowPayloadFeatureMember )? ( 'from' MessageEventMember 'to' MessageEventMember )? | MessageEventMember 'to' MessageEventMember` and MessageEvent = OwnedReferenceSubsetting (kebnf:826-838). FlowPayloadFeatureMember is already read by flow_usage -- reuse it. The two declaration alternatives share no opening token cleanly (both may open on a name), so decide on whether a `to` follows a whole reference, as InterfaceUsageDeclaration does. Deviation MessageKeyword is xtext_only/follow_spec: add no production for it. A StructureUsageElement: add at_message to usage_element_of_class, at_sysml_keyword_member and at_source_succession_member (Interaction Example-2 writes `then message ...`), and remove Message from the NOT-marked list in variant_usage_element's comment. Get the metaclass (FlowUsage? check the wiki) and its constraints with sysml-v2-wiki-navigator before coding. AFTER THAT, by the same first-error measure (2026-09-23): `satisfy` 8 (SatisfyRequirementUsage, 8.2.2.21.2, kebnf:1482, a pending deviation site -- deviation SatisfyRequirementUsage is follow_xtext, read it before coding), IndexExpression `#(` 4, `succession` 3, `concern` 3, `allocation` 3, `allocate` 3, then `view`, `terminate`, `loop`, `for` and `[@` prefix metadata on a multiplicity at 2 each.\n\nNEWEST, 2026-09-22 (seventh close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 342 of 554 units, 180 of 253 .sysml and 19 of 58 .kerml accepted, 218 rejection files caught, last code commit 29b69de, everything pushed to origin/main.\n\nDONE SINCE THE SIXTH CLOSE (four commits, each through a spec-conformance review whose findings were fixed before commit): 23e3406 retired six absence rejections of `#`; 4d7af66 the `#` prefix layer (+21 units, +5 files; EnumeratedValue SITED); 95d9148 interfaces (+14 units, +8 files; review caught PortUsage cited as 8.3.12.5, it is 8.3.12.6 receipt 542cf245); 29b69de library packages (+1 unit, +5 files; LibraryPackage SITED). Deviation sites: 12 sited, 10 pending. Lesson: a mutation of a READER can hide behind its RECOGNISER -- `interface a[1] to b;` was refused by the lookahead before interface_end ran, so the test needed the `connect` form to hold the reader; plant mutations in both halves.\n\nNEXT: EventOccurrenceUsage@sysml (SysML 8.2.2.9.2), the first error in 15 of the 73 rejected .sysml files (e.g. training/27. Occurrences/Interaction Example-2.sysml, examples/Arrowhead Framework Example/AHFSequences.sysml): `OccurrenceUsagePrefix 'event' ( OwnedReferenceSubsetting FeatureSpecializationPart? | 'occurrence' UsageDeclaration? ) UsageCompletion` -- a reference alternative told from `event occurrence` on its one reserved keyword, as RequirementVerificationUsage's are; it is a StructureUsageElement, so add it to usage_element_of_class, at_sysml_keyword_member and at_source_succession_member (training/27 writes `then event occurrence ...`). Retire any absence rejection of `event` first (grep tests/rejection and tests/parser.rs). AFTER THAT, by the same first-error measure: `individual` 9 (IndividualDefinition, IndividualUsage, PortionUsage, 8.2.2.9.1-2 -- note OccurrenceDefinitionPrefix already reads `individual`, the gap is the keywordless forms), `satisfy` 8 (SatisfyRequirementUsage, 8.2.2.21.2, a pending deviation site), IndexExpression `x#(1)` 4 (KerML 8.2.5.8.2, the expression layer, NOT prefix metadata), `snapshot`/`timeslice` 6 (PortionUsage), `allocate` 3, `concern` 3.\n\nNEWEST, 2026-09-22 (sixth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 306 of 554 units, 162 of 253 .sysml and 19 of 58 .kerml accepted, 210 rejection files caught, last code commit aa81590, everything pushed to origin/main.\n\nDONE SINCE THE FIFTH CLOSE (nine commits, each through a spec-conformance review whose blocking findings were fixed before commit): the case layer whole -- bb69266 analysis/case (+6 files), bff74cc ActorMember, 4e30f08 UseCase* (+1), 2296540 IncludeUseCaseUsage (+3), 875edec VerificationCase* (+1), 6448723 RequirementVerificationMember (+2) -- and aa81590 the metadata layer less `#` (+10). Deviation sites: CaseBodyItem and MetadataUsageDeclaration SITED; AnnotatingElement ADJUDICATED to need no site (the deviation changes the element kind, not the text, under ADR-0015's resolution; its register problem statement corrected) -- 10 sited, 12 pending. Recovery lessons: when a production lands, check what recovery restarts on (a `case` after an unread `use` once read Annex A's use cases as cases); and a SysML relationship body now scopes comment significance per annotation.\n\nNEXT: THE `#` PREFIX, SysML 8.2.2.27 with 8.2.2.6.1-2. PrefixMetadataMember@sysml `'#' ownedRelatedElement = PrefixMetadataUsage`, PrefixMetadataUsage `ownedRelationship += OwnedFeatureTyping` (the whole typing, chain included, per deviation PrefixMetadataTyping-chain, follow_spec), UsageExtensionKeyword and DefinitionExtensionKeyword (each `ownedRelationship += PrefixMetadataMember`), and the two productions that exist for them: ExtendedDefinition `BasicDefinitionPrefix? DefinitionExtensionKeyword+ 'def' Definition` and ExtendedUsage `UnextendedUsagePrefix UsageExtensionKeyword+ Usage` (8.2.2.6.1, 8.2.2.6.4). Then walk every production that writes `UsageExtensionKeyword*`/`DefinitionExtensionKeyword*` inline and was left UNMARKED for that alone, and mark it once its `*` is read: MetadataDefinition, MetadataUsage, EnumerationDefinition, SubjectUsage, ActorUsage, ObjectiveRequirementUsage, RequirementConstraintUsage and RequirementVerificationUsage (their `UsageExtensionKeyword+` alternatives too), the prefix productions DefinitionPrefix, UsagePrefix, OccurrenceUsagePrefix, ControlNodePrefix -- and the skip_* recognisers, which today do NOT look past a `#`, must then do so, or the recognisers and productions disagree (the progress guard hides that; see pending decision progress-guard-masks-mismatch). The pending deviation site EnumeratedValue (adds UsageExtensionKeyword*) must be sited in the same commit. Retire the absence rejections first: grep tests/rejection and tests/*.rs for `#` (subject-usage-carries-no-prefix-metadata.sysml, requirement-constraint-usage-prefix-metadata-is-not-implemented.sysml, and the `#m` cases added today in parser.rs). Measure first by blanking `#Name` tokens in rejected files; 14a-Language Extensions already parses. AFTER THAT, by the last measure: interface 7 (8.2.2.14), library packages 4, allocation usage 4, view/viewpoint 4; and the three pending decisions added today.\n\nNEWEST, 2026-09-22 (fifth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 289 of 554 units, 139 of 253 .sysml and 19 of 58 .kerml accepted, 204 rejection files caught, last code commit ffefd20, everything pushed to origin/main.\n\nDONE SINCE THE FOURTH CLOSE, at the owner's direction: ADR-0022 (accepted), DEVIATION DIAGNOSTICS AND STRICT MODE (5193fb1, 42d730f, ffefd20). The parser attaches `PARSE-DEVIATION` (Severity::Info) wherever text parses only because of a register entry that departs from the specification (decision follow_xtext, follow_corpus or the NEW follow_spec_example); Parse::errors() is unchanged, Parse::deviations() holds the notes, Parse::is_spec_conformant() asks for neither, and `sv2 parse --strict` fails on them. Eight sites: SendNode, OccurrenceUsagePrefix, DefaultReferenceUsage, EntryTransitionMember, FlowEndSubsetting, AnnotatingMember, DefinitionElement (`allocation def`) and the new ConnectorEnd-trailing-multiplicity, which reads 7.13.2's `[1] hub ::> mainSwitch[1]` as an OwnedMultiplicity after the reference, SysML only. Measured: of 158 accepted corpus files, 128 are spec-conformant and 30 depend on a deviation (FlowEndSubsetting 18, OccurrenceUsagePrefix 5, DefaultReferenceUsage 4, EntryTransitionMember 3). Strict mode sees EXTENSIONS only: a deviation that restricts the specification rejects text, which has no tree to carry a note.\n\nA NEW STANDING RULE THE GATE ENFORCES (scripts/check_deviation_sites.py): every departing register entry is either SITED -- a `note_deviation(\"NAME\", ...)` call under a `// deviation: NAME` marker, firing only on the text the deviation adds -- or LISTED with a reason in .claude/state/deviation-sites-pending.txt, never both. So WHEN A CONSTRUCT IMPLEMENTS TEXT A PENDING DEVIATION ADDS, add the site and its test in the same commit (a pair in each_deviation_site_fires_on_the_text_it_admits_and_no_other: the admitted text, and a conformant twin that must carry no note) and delete the pending line. Fourteen are pending, and the analysis case below meets one: CaseBodyItem (follow_xtext: 8.2.2.22 omits ReturnParameterMember, so `return` in a case body is a departure -- site it where CaseBody reads a return). Other pending entries and the constructs that will meet them: LibraryPackage (library packages), MetadataUsageDeclaration and AnnotatingElement (metadata), EnumeratedValue and every `#` prefix (UsageExtensionKeyword), SatisfyRequirementUsage, FramedConcernUsage, ForVariableDeclarationMember, ExpressionBody (BodyExpression), and KerML's Step, Expression, BooleanExpression, Invariant, FlowDeclaration. The script cannot check that a listed REASON is true -- the first review of ADR-0022 found DefinitionElement listed as unimplemented while `allocation def` was read -- so ask it in every conformance review.\n\nNEXT: THE ANALYSIS CASE, unchanged from the fourth close below: SysML 8.2.2.23 over the case layer 8.2.2.22 -- AnalysisCaseDefinition, AnalysisCaseUsage, CaseBody, CaseBodyItem, ObjectiveMember (and CaseDefinition/CaseUsage nearly free) -- 9 files by blanking `analysis` statements (defs and usages, both unimplemented). Re-host the bodies before believing 9; model CaseBody as a Body variant beside Body::Calculation; site CaseBodyItem as above. Then metadata 8, interface 7, then library packages, use case, allocation usage and view/viewpoint at 4 each, verification 3.\n\nNEWEST, 2026-09-22 (fourth close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 289 of 554 units, 139 of 253 .sysml and 19 of 58 .kerml accepted, 204 rejection files caught, last code commit db4f468, everything pushed to origin/main.\n\nDONE SINCE THE THIRD CLOSE: ConnectionUsage (2603807 and 25a9862 retiring absences, 4fd3c91; .sysml 125 to 134) and EndUsagePrefix (29b560e retiring, db4f468; 134 to 139). Both landed EXACTLY on their predictions, but only after the connection prediction was CORRECTED: the third close's '13' had blanked `connection def` bodies, which write `end ...` (EndUsagePrefix), so it measured two constructs at once. THE BLANKING PATTERN MUST MATCH ONLY THE PRODUCTION BEING IMPLEMENTED (e.g. `connection(?!\\s+def)`), and any keyword shared with an implemented or separate production must be excluded. Also learned: search for absence rejections by the CONSTRUCTS the change reads, not just its keywords -- the cross multiplicity's absences were written on succession and binding ends (`first [1] a then b;`) and were found only by the tests failing, which forced a stash-and-retire.\n\nNEXT: THE ANALYSIS CASE, SysML 8.2.2.23 with the case layer 8.2.2.22 it stands on. Measured 2026-09-22 by blanking every statement opening on `analysis` (defs AND usages -- both unimplemented, so here that is right): 9 files -- training/{Analysis Case Definition Example, Trade Study Analysis Example}, validation/{10a-Analysis, 10b-Trade-off Among Alternative Configurations, 10d-Dynamics Analysis}, examples/{AnalysisTest, TradeStudyTest, AnalysisIndividualExample, EVSample}. UPPER BOUND: re-host the bodies before believing it (10d also writes `->including`, FunctionOperationExpression, absent). Productions: AnalysisCaseDefinition@sysml `OccurrenceDefinitionPrefix 'analysis' 'def' DefinitionDeclaration CaseBody`; AnalysisCaseUsage@sysml `OccurrenceUsagePrefix 'analysis' ConstraintUsageDeclaration CaseBody`; CaseBody@sysml `';' | '{' CaseBodyItem* ResultExpressionMember? '}'`; CaseBodyItem@sysml `ActionBodyItem | ReturnParameterMember | SubjectMember | ActorMember | ObjectiveMember`; ObjectiveMember@sysml `MemberPrefix 'objective' ObjectiveRequirementUsage` (read its unit); ActorMember is the use-case layer and may be deferred. CaseBody is CalculationBody plus subject/actor/objective items: model it as a new Body variant beside Body::Calculation (ends_in_result_expression, admits_action_body_item), and CaseDefinition/CaseUsage (`case`) come nearly free. Run deviations.json for every name. THEN, by the same measure: metadata 8 (`metadata def` and `@` usages, 8.2.2.27), interface 7 (8.2.2.14), then library packages, use case, allocation and view/viewpoint at 4 each; verification 3.\n\nKERML FOLLOW-UP recorded this session: KerML's multiplicity (OwnedMultiplicity@kerml = OwnedMultiplicityRange = MultiplicityBounds, `[` bounds `]`) is unimplemented, which is why the shared OwnedCrossMultiplicity(Member) and ConnectorEnd stay unmarked and tests/rejection/kerml-connector-end-cross-multiplicity-is-not-implemented.kerml stays; 33 KerML corpus files write a bracketed multiplicity. 7.13.2's n-ary example `[1] hub ::> mainSwitch[1]` was ADJUDICATED later the same day: deviation ConnectorEnd-trailing-multiplicity, follow_spec_example (see the fifth close).\n\nNEWEST, 2026-09-22 (third close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 281 of 554 units, 125 of 253 .sysml and 19 of 58 .kerml accepted, 200 rejection files caught, last code commit aabb2e0 (retirement ahead of it 64b05bf), everything pushed to origin/main.\n\nDONE THIS SESSION: the send/accept/assign action nodes and `new`, as one construct. .sysml 106 to 125: all 15 files the blanking predicted, PLUS four it did not name (3a-Function-based Behavior-1/-2, training/21 Messaging Example and Messaging with Ports), which write `action NAME accept ...` in an action body -- AcceptNode was implemented but was not in what was blanked. LESSON: blank exactly the construct you will implement, or the 'upper bound' is not one. The conformance review's one blocking finding was a false comment ('`new` is reserved in both'); fixing it surfaced pending decision [keyword-table-per-language].\n\nNEXT: THE CONNECTION LAYER, SysML 8.2.2.13.1. Measured 2026-09-22 by BLANKING every statement opening on `connection` or `connect` (a MemberPrefix/usage-prefix keyword run allowed before it): 13 rejected files come out clean, the most of any construct -- all .sysml: training/09. Connections/Connections Example; validation/{2c-Parts Interconnection-Multiple Decompositions, 3c-Function-based Behavior-structure mod-1, 3c-...-structure mod-2, 7a1-Variant Configuration - General Concept-a, 14b-Language Extensions}; examples/Association Examples/{ProductSelection_OwnedEnds, ProductSelection_N_ary, ProductSelection_UnownedEnds}; examples/Simple Tests/{FeaturePathTest, AliasTest}; examples/v1 Spec Examples/8.4.1 Wheel Hub Assembly/{Wheel Package, Wheel Package - Updated}. UPPER BOUND. KerML's Connector (Connector@kerml, `connector`) is a different production and bought nothing in this measure. Productions, all unimplemented: ConnectionUsage@sysml `OccurrenceUsagePrefix ( 'connection' UsageDeclaration ValuePart? ( 'connect' ConnectorPart )? | 'connect' ConnectorPart ) UsageBody`; ConnectorPart@sysml `BinaryConnectorPart | NaryConnectorPart`; BinaryConnectorPart@sysml `ConnectorEndMember 'to' ConnectorEndMember`; NaryConnectorPart@sysml `'(' ConnectorEndMember ',' ConnectorEndMember ( ',' ConnectorEndMember )* ')'`. ConnectorEndMember is implemented and ConnectorEnd is read but unmarked (its OwnedCrossMultiplicityMember is missing -- `end [1] part bead` in Connections Example is a different gap, EndUsagePrefix, 6 first-failures): read both before relying on them. ConnectionDefinition is already implemented. `connect` also opens InterfaceUsage's connector part (8.2.2.14), so keep the recogniser keyed on the production, not the word. Run deviations.json for every production named, and tests/rejection + tests/*.rs for an absence rejection to retire first.\n\nAFTER THAT, by the same measure (2026-09-22): analysis 9 files (AnalysisCase*, 8.2.2.23), metadata 8 (`metadata def` and `@` usages, 8.2.2.27), interface 6 (8.2.2.14), then library/standard packages, use case, allocation and view/viewpoint at 4 each; event 2, terminate 2, loops 2, message 1, satisfy 1. First-failure counts rank interface first (15) -- blanking shows most of those files also fail elsewhere, so trust blanking.\n\nTHE METHOD THAT HELD is unchanged; read it in the previous paragraph's copy below. The measurement script used this session (statement blanking with comment/string masking) was a scratch file; the method is: for each rejected corpus file, space out every statement whose first keyword (after prefix keywords) is in the candidate set, up to its `;` or balanced `}`, keeping newlines, then parse.\n\nNEWEST, 2026-09-22 (second close) -- read this first; the paragraphs after it are history. STATE AT CLOSE: gate green, 263 of 554 units, 106 of 253 .sysml and 19 of 58 .kerml accepted, 193 rejection files caught, last code commit c9fa442, everything pushed to origin/main. The working tree was clean before this close-out.\n\nNEXT: THE SEND AND ASSIGNMENT ACTION NODES AND THE `new` EXPRESSION, as one construct, because the corpus writes them together (`do send new ControllerStartSignal() to controller`). Predicted by BLANKING every statement containing `send` or `assign` and rewriting `new X(...)` as the implemented invocation `X(...)`: 15 rejected files come out clean -- examples/Simple Tests/{AssignmentTest, DefaultValueTest, ParameterTest, StateTest}; examples/Geometry Examples/{CarWithShapeAndCSG, SimpleQuadcopter}; examples/State Space Representation Examples/CartSample; training/25. Transitions/{Change and Time Triggers, Local Clock Example, Transition Actions}; training/31. Constraints/Time Constraints; validation/03-Function-based Behavior/3c-...structure mod-3; validation/05-State-based Behavior/{5-...-1, 5-...-1a, 5-...-2}. THAT IS AN UPPER BOUND: blanking a whole statement removes what recovery would have tripped on, and several FAILs after blanking are a bare `then` whose behaviour item was blanked (ActionTest:51) -- re-host each statement KEEPING its own opening tokens before believing a number. The productions, all @sysml unless marked:\n  SendNode (kebnf:1068, 8.2.2.17.4) `OccurrenceUsagePrefix ActionUsageDeclaration? 'send' ( NodeParameterMember SenderReceiverPart? | EmptyParameterMember SenderReceiverPart )? ActionBody` -- deviations.json SendNode is conflict/follow_xtext: read ActionNodeUsageDeclaration (`action publish send ...`), not ActionUsageDeclaration.\n  SendNodeDeclaration (kebnf:1074) `ActionNodeUsageDeclaration? 'send' NodeParameterMember SenderReceiverPart?`\n  SenderReceiverPart (kebnf:1078) `'via' NodeParameterMember ( 'to' NodeParameterMember )? | EmptyParameterMember 'to' NodeParameterMember`\n  AssignmentNode (kebnf:1104, 8.2.2.17.5) `OccurrenceUsagePrefix AssignmentNodeDeclaration ActionBody`\n  AssignmentNodeDeclaration (kebnf:1108) `ActionNodeUsageDeclaration? 'assign' AssignmentTargetMember FeatureChainMember ':=' NodeParameterMember`, with AssignmentTargetMember (kebnf:1114) and its AssignmentTargetParameter -- read their bodies first.\n  StateSendActionUsage, StateAssignmentActionUsage (kebnf:1261/1264) and TransitionSendActionUsage, TransitionAssignmentActionUsage (kebnf:1346/1349), all spec_only/follow_spec: once the two declarations exist, add them to `state_action_usage` and `effect_behavior_member`, whose `Some(_)` arm today reports \"send and assignment actions are not implemented\"; then StateActionUsage and EffectBehaviorUsage can be MARKED.\n  ConstructorExpression (KerML-textual-bnf.kebnf:1215, KerML 8.2.5.8.2, shared unit) `'new' InstantiatedTypeMember ConstructorResultMember`, with ConstructorResultMember, ConstructorResult (= ArgumentList, implemented) and InstantiatedTypeMember (`InstantiatedTypeReference | OwnedFeatureChainMember`; InstantiatedTypeReference is already implemented). The parser's PrimaryExpression comment near the InvocationExpression (grep `ConstructorExpression` in parser.rs) lists where it goes. `new` is reserved in SysML (kebnf:20); check KerML.\n  SendNode and AssignmentNode are ActionNodes, ActionBehaviorMember's second alternative (8.2.2.17.1): wire them where ControlNode is (`at_control_node`, `membership`'s ActionNode arm, `Body::member`, `behaviour_targets`), and AcceptNode (kebnf:1018, `OccurrenceUsagePrefix AcceptNodeDeclaration ActionBody`) comes nearly free, since AcceptNodeDeclaration and ActionNodeUsageDeclaration exist. ActionNode stays unmarked until Terminate/If/While/For are read.\n\nTHE METHOD THAT HELD, in order, per production or construct: (1) jq the units, AND deviations.json FOR EVERY PRODUCTION IN THE CONSTRUCT (this session implemented six state productions before reading their entries; they happened to agree), and `tests/rejection` + tests/*.rs for an ABSENCE rejection -- retire any in ITS OWN COMMIT first, gate green there (precedents 5c4ca6f, e793207, 4fc015e); (2) retrieve semantics with the sysml-v2-wiki-navigator closure.py (wiki at ~/.sysml-v2-llm-wiki/wiki, KerML at ~/.kerml-llm-wiki/wiki; sqlite3 CLI absent, use python sqlite3) and grep each receipt in .claude/state/wiki-receipts.json; (3) predict by BLANKING and re-hosting -- blanking `state def` alone UNDER-predicted (2 vs 4) because files declared their states as usages: blank the whole construct, usages included; (4) failing tests first, citing corpus lines or clause examples, never program output; (5) node kinds in scripts/gen_syntax_kinds.py (backtick every production name in its doc string, or clippy's doc_markdown fails the generated file), then the parser -- a new SysML member goes in at_sysml_keyword_member (or, if it is a body item `membership` does not read, in body_specific_item plus at_member_keyword by hand), usage_element_of_class, at_source_succession_member for behaviour usages; (6) rejection files one per rule, each proved by a mutation that makes it LEAK -- a mutation that leaves the file rejected for another reason proves nothing and must be replaced (the dependency `from` rule's first one did); re-run the mutations after any refactor; (7) corpus_sweep --record, bnf_coverage, regen_state, gate, then the spec-conformance-reviewer (.claude/agents/spec-conformance-reviewer.md run as a general-purpose agent), and where clause-7 prose and the BNF disagree, the grammar-adjudicator (.claude/agents/grammar-adjudicator.md, general-purpose) and a deviations.json entry. EDIT deviations.json AS TEXT: it is not sorted and jq rewrites its formatting (a sort_by produced a 1168-line diff this session); insert beside its neighbour and check `jq empty` and scripts/check_citations.py. Clippy's cognitive_complexity and too_many_lines are gate failures: split the function (body_specific_item came out of body_element that way) or the test, never #[allow]. Run cargo and the gate under `systemd-run --user --scope -p MemoryMax=8G -p MemorySwapMax=0`. Validate state with `uv run python`. AFTER the send/assign construct, by the blanking measure: the seven `exhibit` files now fail later, mostly at `view`, `connect`, `interface`, `message` and `for` -- measure them again.\n\nEARLIER, 2026-09-22 (history). THIS SESSION, after the enumeration: VariantUsageMember and VariantReference (c4875cb, 90 to 94 .sysml); Dependency in both grammars (0d77643, 94 to 97); KerML RelationshipBody (0282090, .kerml 18 to 19, Dependencies.kerml); ConjugatedPortTyping (45aa4d8, 97 to 100); the state layer as one construct (e793207 retiring `state def S;`, 3f84a6c, 100 to 104 -- four where blanking predicted two); and the rest of it, state actions, entry transitions, effects, triggers and exhibit (4fc015e retiring two absences, c9fa442, 104 to 106). Units 224 to 263. One conflict adjudicated into deviations.json, TransitionUsage (follow_spec; OnOff4 guard-before-accepter, and the `;` after an effect filed as SYSML21-450); the variant-reference multiplicity conflict is still only a comment, and a pending decision. Every commit had a conformance review; two found blocking uncited-normative claims (implied specializations cited under the wrong constraints for StateUsage and TransitionUsage), both fixed before commit.\n\nEARLIER, 2026-09-22 (history). THE ENUMERATION DEFINITION IS DONE (SysML 8.2.2.8): 221 to 224 of 554 units -- EnumerationBody, EnumerationUsageMember and AnnotatingMember marked; EnumerationDefinition and EnumeratedValue NOT, each carrying unimplemented prefix metadata in its own rule (DefinitionExtensionKeyword*, and UsageExtensionKeyword* by the follow_xtext deviation), as DefinitionPrefix is not. .sysml 86 to 90, the four predicted by BLANKING (EnumerationTest, Enumeration Definitions-1 and -2, 15_10-Primitive Data Types) -- but only after a fix the first sweep forced: it bought three, because at_enumerated_value did not admit a value with NO declaration, `= 60.0;` (EnumerationTest.sysml:48-50), which `Usage = UsageDeclaration UsageCompletion` allows since the whole declaration is optional (8.2.2.6.2). The body re-hosting that fed the prediction had written each value as `attribute ...`, and the keyword made the bare form readable -- A RE-HOSTED TEXT MUST KEEP THE CONSTRUCT'S OWN OPENING TOKENS, or it tests a different recogniser. EnumerationBody has its own item loop with the progress guard, not body_elements, because only enumerated values and annotations are items there (7.8.2). The absence rejection was retired ahead (5c4ca6f). Five rejection files, four proved by mutation.\n\nEARLIER, 2026-09-22 (history). RequirementUsage@sysml is DONE (8.2.2.21.2): 220 to 221 of 554 units, .sysml 82 to 86. THE PREDICTION MISSED, SIX PREDICTED AND FOUR BOUGHT, AND THE METHOD WAS WRONG, NOT THE COUNTING: re-hosting a construct's BODY checks what is inside it, but not what recovery swallowed AFTER it. recover_statement stops at at_member_keyword, and an unimplemented construct is not in that set, so recovery from the first `requirement` usage ran on through the statements behind it and hid their diagnostics: examples/Requirements Examples/VehicleRequirementDerivation also writes `#derivation connection {` (line 34, prefix metadata) and training/34. Verification/Verification Case Definition Example `verification def VehicleMassTest {` (line 18). THE SOUND METHOD, used for the measurement below: copy the file, BLANK every statement of the target construct (spaces for its bytes, newlines kept), parse the rest, and count the file only if nothing is left; then separately re-host the construct's bodies. The assert, calc and constraint predictions held, but that method could not have seen this failure either. The absence rejection was retired in its own commit ahead (df81503); its header had quoted the declaration as CalculationUsageDeclaration where the clause writes ConstraintUsageDeclaration. Three rejection files; two proved by mutation, one of them by swapping RequirementBody for CalculationBody. NEXT, MEASURED 2026-09-22 BY BLANKING: four constructs tie at four rejected files each, and blanking separates them -- `enum def` 4 of 4 clean once removed (examples/Simple Tests/EnumerationTest, training/06. Enumeration Definitions/Enumeration Definitions-1 and -2, validation/15_10-Primitive Data Types); `variant` 4 of 4; `port ... : ~T` 3 of 4 (training/13 Flow Definition Example also writes `end port`); `state def` 2 of 4 (StateTest and State Actions also write `state` usages). TAKE `enum def`: EnumerationDefinition@sysml, EnumerationBody@sysml, EnumerationUsageMember@sysml and EnumeratedValue@sysml (SysML 8.2.2.8) -- `DefinitionExtensionKeyword* 'enum' 'def' DefinitionDeclaration EnumerationBody`, the body `';' | '{' ( AnnotatingMember | EnumerationUsageMember )* '}'`, a member `MemberPrefix EnumeratedValue`, a value `'enum'? Usage`. Four units that are one construct: a definition without its body reads nothing, so they land together, and the add-production skill's one-production rule is waived for that reason and should be said so in the commit. 4 is still an upper bound until each enum body is read -- a value may carry a ValuePart or a body. DefinitionExtensionKeyword* is prefix metadata, unimplemented everywhere; the definition is marked only if its own parts are read, as the classifiers are. EnumerationUsage is already read (a SIMPLE_USAGE). Then `variant` (VariantUsageMember, VariantUsageElement, VariantReference; 4 of 4) and ConjugatedPortTyping@sysml (8.2.2.12, 3 of 4, the smallest).\n\nEARLIER, 2026-09-22 (history). ConstraintUsage@sysml is DONE (8.2.2.20): 219 to 220 of 554 units, .sysml 75 to 82 -- EXACTLY the seven predicted, every body re-hosted as a `constraint def` body first. The absence rejection and its two unit-test twins were retired in their own commit ahead (5a5a82e), both inputs return ACCEPTED, and constraint_usage_declaration's comment, stale since the assertion landed, was corrected there. A constraint usage inside a `constraint def` body parsed WITHOUT any calculation-body change, because the production went into at_sysml_keyword_member: the one-list fix working as intended. Two rejection files; the body rule proved by mutation. STILL SEPARATE LISTS, and each needs the new production added by hand: at_member_keyword (recovery's stopping set), at_source_succession_member (the behaviour usages a `then` may precede) and usage_element_of_class (the dispatch). Folding the first two onto shared recognisers is worth a look before the next behaviour usage. NEXT, MEASURED 2026-09-22: RequirementUsage@sysml (SysML 8.2.2.21.2), `OccurrenceUsagePrefix 'requirement' ConstraintUsageDeclaration RequirementBody`, heads SIX .sysml files whose every diagnostic is a `requirement` usage line: examples/Requirements Examples/HSUVRequirements (+1) and VehicleRequirementDerivation, training/32. Requirements/Requirement Groups and Requirement Usages, training/34. Verification/Verification Case Definition Example, validation/13-Model Containment/13a-Model Containment. Both parts are built: constraint_usage_declaration, and requirement_body from RequirementDefinition. AN UPPER BOUND until each body is re-hosted as a `requirement def` body; RequirementBody reaches SubjectMember, RequirementConstraintMember and nested requirements, and RequirementVerificationMember (`verify`) is unimplemented -- expect it to cut the count. FIRST, IN ITS OWN COMMIT: retire tests/rejection/requirement-usage-is-not-a-requirement-definition.sysml and any unit-test line asserting a requirement usage rejected by absence. Then, four each: `port` (ConjugatedPortTyping@sysml, 8.2.2.12, `: ~FuelPort`), `enum def`, `state def`, `variant`.\n\nEARLIER, 2026-09-22 (history). CalculationUsage@sysml is DONE (8.2.2.19): 218 to 219 of 554 units, .sysml 71 to 75 -- EXACTLY the four predicted (examples/Simple Tests/CalculationTest, examples/Analysis Examples/Dynamics, training/30. Calculations/Calculation Usages-1 and -2). The re-hosting method cut the upper bound of five to four BEFORE coding: examples/State Space Representation Examples/CartSample's three `calc :>> ...` bodies use `new` (InstantiationExpression, unimplemented) and it stays rejected. The absence rejection calculation-usage-is-not-a-calculation-definition.sysml was retired in its own commit ahead (be11cfc), per the tests rule, and its input returns asserted ACCEPTED, with the usage/definition split asserted by tree shape. A REJECTION FILE WAS REJECTED FOR THE WRONG REASON AND A MUTATION CAUGHT IT: `calc c { a + b attribute x; }` was meant to hold 'the result expression is last' (8.2.2.19), but a body item that opens on a NAME is decided by usage_completion_follows, which sees the `;` and reads `a` as a keywordless usage -- so the mutation that admitted items after the result left it rejected. Written `(a + b) attribute x;`, it reaches the rule, and the mutation now leaks it. Lesson for every rejection file: a sweep that reports 'caught' says the file is rejected, not WHY; only a mutation of the rule it names says that. A DEFECT FOUND AND FIXED AFTER IT: at_result_expression kept its own list of what starts a calculation-body item, and SuccessionAsUsage, BindingConnectorAsUsage, AssertConstraintUsage and CalculationUsage had each been added to at_member_element and not to it -- so `first a then b;`, `bind`, `assert constraint` and `calc` inside a calc def, constraint def, calc usage or assertion body were read as the start of the result expression. Both now ask at_sysml_keyword_member, one list. A stale test that asserted `constraint def C { first a then b; }` rejected, on the claim that the control-flow layer was absent, was dropped in its own commit ahead (3ac64b9) and the input returns ACCEPTED. No corpus file moved. ANY NEW SYSML MEMBER GOES IN at_sysml_keyword_member, and nowhere else. NEXT, MEASURED 2026-09-22 over every rejected file: ConstraintUsage@sysml (SysML 8.2.2.20), `OccurrenceUsagePrefix 'constraint' ConstraintUsageDeclaration CalculationBody`, heads SEVEN .sysml files whose every diagnostic is a `constraint` usage line: examples/Analysis Examples/Turbojet Stage Analysis, examples/Mass Roll-up Example/MassConstraintExample, examples/Simple Tests/ConstraintTest (+1), training/31. Constraints/Constraint Assertions-2, Constraints Example-1 and Constraints Example-2, and validation/15-Properties-Values-Expressions/15_03-Value Expression. The same shape as AssertConstraintUsage's second alternative less `assert`, over the same constraint_usage_declaration and calculation_body; told from ConstraintDefinition by the `def`, as calc is. AN UPPER BOUND until each body is re-hosted as a `constraint def` body. FIRST, IN ITS OWN COMMIT: retire tests/rejection/constraint-usage-is-not-a-constraint-definition.sysml -- which says so itself, 'removing this rejection is the next step after this one' -- and any unit test line asserting `constraint c { ... }` rejected, then bring the input back ACCEPTED. The other seven-file lever is `analysis` (AnalysisCaseDefinition and AnalysisCaseUsage, SysML 8.2.2.23), a larger layer. Then `requirement` 6, `enum def` 4, `state def` 4.\n\nEARLIER, 2026-09-21 (history). AssertConstraintUsage@sysml is DONE (8.2.2.20): 217 to 218 of 554 units, .sysml 64 to 71 -- EXACTLY the seven predicted. The upper bound was turned into a prediction by re-hosting every `assert` block of the seven files as a `constraint def` with the same CalculationBody and parsing that: every body read, and every enclosing body (item/part/action/attribute def, part and attribute usage) is a DefinitionBody that admits an OccurrenceUsageMember. The recogniser skips OccurrenceUsagePrefix and the production reads occurrence_usage_prefix -- the pairing checked, not assumed -- and declines `assert not? satisfy`, SatisfyRequirementUsage (8.2.2.21.2, unimplemented). at_source_succession_member lists behaviour usages by name, so the assertion had to be added there for `then assert constraint ...` to reach it. Four rejection files, three proved by mutation. NEXT, MEASURED 2026-09-21 the same way: CalculationUsage@sysml (SysML 8.2.2.19), `OccurrenceUsagePrefix 'calc' ActionUsageDeclaration CalculationBody`, heads FIVE .sysml files whose every diagnostic is a `calc` line: examples/Simple Tests/CalculationTest, training/30. Calculations/Calculation Usages-1 and Calculation Usages-2 (no other diagnostic), examples/Analysis Examples/Dynamics (+2 more calc lines) and examples/State Space Representation Examples/CartSample (`calc :>> getDerivative {`, +1). AN UPPER BOUND, as `assert` was: re-host each `calc` block's body as a `calc def` body and parse it before claiming any file -- CalculationBody reaches ReturnParameterMember, which is implemented, but a body may hold something that is not. Everything under it is built: calculation_body, occurrence_usage_prefix; ActionUsageDeclaration is what action_usage reads -- check it is a function of its own. Mind the prefix pairing again. After it, by the same measure, four each: `port` (the conjugated typing `: ~FuelPort`, ConjugatedPortTyping@sysml, 8.2.2.12, unimplemented; PortUsage itself is read), `enum def`, `state def`, `variant`.\n\nEARLIER, 2026-09-21 (history). THE BINDING PAIR IS DONE. BindingConnector@kerml and BindingConnectorDeclaration@kerml (KerML 8.2.5.5.2): 215 to 217 of 554 units, acceptance UNCHANGED at 64/253 .sysml and 18/58 .kerml -- exactly the zero predicted, and every `binding` line in the five .kerml files that write one now parses (Behaviors, Connectors, FeatureChains, Features, ExtendedOccurrences); what still rejects them is other constructs. Two rejection files, both proved by mutation. THE UNIT WAS WRONG ON ONE POINT: BindingConnectorDeclaration@kerml.json says 'No corpus file uses the empty form', but examples/Simple Tests/Connectors.kerml:16-19 writes `binding { end feature references a; end feature references b; }` -- the empty declaration with its ends as end features in the body. It is now a positive test; the unit is machine-owned and was not edited. validateBindingConnectorIsBinary is NOT held by the KerML grammar (it is by SysML's): `binding;` parses, and the count of ends is left to the layer that has relatedFeatures (ADR-0002). NEXT, MEASURED 2026-09-21 over every rejected corpus file with the current parser: AssertConstraintUsage@sysml (SysML 8.2.2.20), `OccurrenceUsagePrefix 'assert' 'not'? ( OwnedReferenceSubsetting FeatureSpecializationPart? | 'constraint' ConstraintUsageDeclaration ) CalculationBody`, heads SEVEN .sysml files whose every diagnostic is an `assert constraint` line, six with no other diagnostic at all: examples/Simple Tests/TextualRepresentationTest, training/31. Constraints/Analytical Constraints, Constraint Assertions-1 and Derivation Constraints, validation/15_01-Constants, 15_04-Logical Expressions and 15_08-Range Restriction. SEVEN IS AN UPPER BOUND: recovery skips a braced body whole, so the CalculationBody's expressions have never been read in these files -- re-take the prediction by reading each body (`{ totalMass == chassisMass + ... }`, `round(e * 1E20) == ...`, `angle >= 50 ['\u00b0'] and ...`) against the expression layer before coding. ConstraintUsage@sysml is unimplemented too; read both units and decide whether ConstraintUsageDeclaration comes with it. MIND THE PREFIX: unlike the two bindings this one takes OccurrenceUsagePrefix, so its recogniser skips skip_occurrence_usage_prefix and the production must read occurrence_usage_prefix -- the pairing whose mismatch hung the parser. Next by the same measure: `requirement` 6 (a construct inside RequirementUsage bodies), then `metadata`, `port`, `calc` and `state def` at 4 each.\n\nEARLIER, 2026-09-21 (history). BindingConnectorAsUsage@sysml is DONE (8.2.2.13.2): 214 to 215 of 554 units, .sysml 59 to 64 -- EXACTLY the five files predicted (training/12 Binding Connectors Example-1, training/14 Action Definition Example and Action Succession Example-1, validation/01 1d-Parts Tree with Reference, validation/03 3e-Function-based Behavior-item), after every diagnostic line of each was attributed. The recogniser decides on the reserved `binding`/`bind` alone (8.2.2.1.2; no other SysML production opens with either), after skip_basic_usage_prefix, which is exactly what binding_connector_as_usage reads. Six rejection files; three proved by mutation, and the mutation that reintroduced the morning's hang (the recogniser skipping OccurrenceUsagePrefix) was caught by the sweep as 'rejected for a different reason' rather than spinning -- the progress guard doing what [progress-guard-masks-mismatch] says, and the rejection FILE, not the unit test, noticing. NEXT, THE KERML HALF OF THE PAIR: BindingConnector@kerml and BindingConnectorDeclaration@kerml (KerML 8.2.5.5.2), `FeaturePrefix 'binding' BindingConnectorDeclaration TypeBody`, where BindingConnectorDeclaration = FeatureDeclaration ( 'of' ConnectorEndMember '=' ConnectorEndMember )? | ( 'all' )? ( 'of'? ConnectorEndMember '=' ConnectorEndMember )?. It is kerml_succession/succession_declaration with `binding` for `succession`, `of` for `first` and `=` for `then`: take the second alternative when `of` follows the optional `all`, when nothing does (`binding;`, `binding {` -- the corpus writes the latter once), or when a ConnectorEnd and then `=` follow; anything else is a FeatureDeclaration (`binding ab1 : AS of a = b;`). Dispatch it where kerml_succession is: at_kerml_binding beside at_kerml_succession in body_elements' KerML arm, namespace_feature_member, at_member_element and at_member_keyword. READ THE TWO UNITS FIRST. THE PREDICTION IS ZERO, measured 2026-09-21: 8 `binding` lines in 5 .kerml files, and every rejected one has other diagnostics too (Behaviors 1 of 6, Connectors 2 of 7, FeatureChains 1 of 10, Features 2 of 15). It is completeness for the pair, not acceptance; say so in the commit. tests/rejection/binding-connector-as-usage-is-not-kerml.kerml must stay rejected BY RULE -- `bind` is no KerML keyword -- after it lands.\n\nEARLIER, 2026-09-21 (history). THE SUCCESSION PAIR IS DONE: SuccessionAsUsage@sysml (8.2.2.13.3), Succession@kerml and SuccessionDeclaration@kerml (8.2.5.5.3): 211 to 214 of 554 units, .sysml 58 to 59 (training/28. Individuals and Snapshots Example) and .kerml 17 to 18 (examples/Behavior Examples/Camera.kerml) -- the first .kerml move in this run of work, exactly the KerML file predicted. THE SYSML PREDICTION MISSED, the first miss after nine: training/14. Action Succession Example-1 was expected to flip and did not. Its `first focus then shoot;` (line 19) now parses; lines 13 and 23 are `bind focus.scene = scene;` and `bind shoot.picture = picture;`, which the prediction never attributed. The method was right and was not applied to that file: attribute EVERY diagnostic line of a file before counting it. A HANG WAS FOUND AND FIXED on the way: at_succession_as_usage skipped OccurrenceUsagePrefix (past `snapshot`) while succession_as_usage reads only UsagePrefix, so `part def P { snapshot first a then b; }` spun body_elements for ever, growing a diagnostic per pass until the test process was OOM-killed. The recogniser now skips skip_basic_usage_prefix, and commit ac031cf added a progress guard to body_elements (see [progress-guard-masks-mismatch]). NEXT, THE BINDING PAIR, the same shape as the succession pair and over the same parts: BindingConnectorAsUsage@sysml (SysML 8.2.2.13.2) with BindingConnector@kerml and BindingConnectorDeclaration@kerml (KerML 8.2.5.5.2). READ THE THREE UNITS FIRST (.claude/state/grammar/units/BindingConnectorAsUsage@sysml.json and the two @kerml ones); do not write the rule from memory. MEASURED 2026-09-21 over every rejected .sysml file, attributing every diagnostic line: FIVE files fail on nothing but `bind` lines -- training/12. Binding Connectors/Binding Connectors Example-1, training/14. Action Definitions/Action Definition Example, training/14. Action Definitions/Action Succession Example-1, validation/01-Parts Tree/1d-Parts Tree with Reference (a `bind ... {` with a body), validation/03-Function-based Behavior/3e-Function-based Behavior-item (a quoted multi-link feature chain end). Five is an upper bound until each line's full form is checked; re-take it, and take the .kerml side (`binding`) too, before coding. Mirror at_succession_as_usage/succession_as_usage and kerml_succession/succession_declaration; the ends are skip_connector_end, then `=` where the succession has `then`. KEEP THE RECOGNISER'S PREFIX SKIP IDENTICAL TO WHAT THE PRODUCTION READS -- that mismatch is what hung this session. The other lever measured: `state` heads 9 rejected files, but each error sits on a `state def`/`state` header whose whole body recovery skipped, so 9 is an upper bound only; StateDefinition and StateUsage are a layer, not a production.\n\nEARLIER, 2026-09-19 (history). GuardedSuccession is DONE, with GuardedSuccessionMember, FeatureChainMember@sysml and OwnedFeatureChainMember@sysml: 207 to 211 of 554 units, 56 to 58 of 253 .sysml, exactly the two files predicted (DecisionTest, Conditional Succession Example-1) -- the ninth prediction in a row to hold. ActionBodyItem's four alternatives are now ALL read over the elements that exist, which closes the control-flow layer's outer shape; what is left inside it is the seven non-control ActionNodes. THE `first` COLLISION IS SETTLED AND NEEDED NO NEW CODE: at_initial_node_member already required a `;` or `{` after the name, at_guarded_succession_member requires an `if`, and SuccessionAsUsage (8.2.2.13.3, unimplemented) requires a `then` -- three productions disjoint on ONE token, none committing before it. A RUST NAME WAS SCOPED: `feature_chain_member` became `kerml_feature_chain_member`, because SysML 8.2.2.17.5 states a production of the same name with a different body (ADR-0015's case in code rather than in coverage). NEXT, the biggest remaining lever by the same measurement, and it is a KerML/SysML PAIR: SuccessionAsUsage@sysml (8.2.2.13.3, `( 'succession' UsageDeclaration )? 'first' ConnectorEndMember 'then' ConnectorEndMember`) with Succession@kerml and SuccessionDeclaration@kerml (8.2.5.5.2). `first X then Y;` is ONE LINE from flipping training/14. Action Succession Example-1, training/28. Individuals and Snapshots Example, and -- in KerML -- examples/Behavior Examples/Camera.kerml (`succession focusedState then shotState;`). Everything under it is built: ConnectorEndMember, ConnectorEnd, UsageDeclaration. Re-take the prediction over the whole corpus first; a KerML unit means the .kerml side of the sweep can move for the first time in this run of work.\n\nEARLIER, 2026-09-19 (history). DefaultTargetSuccession is DONE, and with it ActionTargetSuccession is MARKED -- all three alternatives read: 205 to 207 of 554 units. Acceptance UNCHANGED at 56 of 253 .sysml, which is exactly what was predicted: over the 7 corpus files with a line-leading `else`, no file's diagnostics were all `else X;` lines. The eighth prediction in a row to hold, and the fourth that was zero. `else` needs no lookahead beyond its keyword: KerML's ConditionalExpression writes the only other one and it stands inside the expression. NEXT, THE ONE THAT BUYS TWO FILES: GuardedSuccessionMember over GuardedSuccession, ActionBodyItem's FOURTH and last alternative (8.2.2.17.8) -- `( 'succession' UsageDeclaration )? 'first' FeatureChainMember GuardExpressionMember 'then' TransitionSuccessionMember UsageBody`, everything under it already built except FeatureChainMember@sysml (`[QualifiedName] | OwnedFeatureChainMember`, 8.2.2.17.5). It buys training/16. Conditional Succession Example-1 (`first focus` NEWLINE `if focus.image.isWellFocused then shoot;`) and, now that `else` lands, examples/Simple Tests/DecisionTest.sysml (its last residue was `public succession S first A1` + `if x == 0 then A2;`). CONFIRM WITH THE METHOD FIRST. THE COLLISION TO SETTLE BEFORE CODING: InitialNodeMember is `'first' [QualifiedName] RelationshipBody` and GuardedSuccession opens `'first' FeatureChainMember GuardExpressionMember`, so `first X` is decided by what follows the name -- a `;` or `{` is the initial node, an `if` is this. at_initial_node_member must decline the guarded form, and DecisionTest:20-21 (`private first A3;` then a guarded target succession on the NEXT line) is the case that proves the two stay apart. Note also the `succession` form: `public succession S first A1 if ... then A2;`.\n\nEARLIER, 2026-09-19 (history). GuardedTargetSuccession is DONE (with GuardExpressionMember, TransitionSuccessionMember, TransitionSuccession, EmptyEndMember): 200 to 205 of 554 units, 54 to 56 of 253 .sysml, exactly the two files predicted (Decision Example, Conditional Succession Example-2) -- the seventh prediction in a row to hold. ActionTargetSuccession stays UNMARKED: DefaultTargetSuccession (`else X;`) is the one alternative left, held by tests/rejection/default-target-succession-is-not-implemented.sysml. THE LOOKAHEAD TO KNOW ABOUT: `if` opens three productions -- GuardedTargetSuccession, IfNode (8.2.2.17.7, an unimplemented ActionNode) and KerML's ConditionalExpression (8.2.5.8.1, a calculation body's result). at_guarded_target_succession scans for a `then` before the first `;`, `{`, `}` or end of input, because no implemented expression contains a `then`. If IfNode lands, that scan is the thing to revisit. NEXT, three candidates, each one file by the same measurement over the 19 files with a line-leading `if`: GuardedSuccessionMember/GuardedSuccession (`first focus if <expr> then shoot;`, ActionBodyItem's FOURTH alternative, 8.2.2.17.8) buys training/16. Conditional Succession Example-1, and NOTE it collides with InitialNodeMember -- `first X` then a guard rather than a `;`; DefaultTargetSuccession plus GuardedSuccession together buy examples/Simple Tests/DecisionTest.sysml (its residue is `else A3;` and `public succession S first A1 if ...`); TerminateNode (8.2.2.17.4) buys training/19. Terminate Actions Example-1 (`then terminate;`, `action stop terminate;`). Re-take the prediction before coding. A REJECTION BY ABSENCE CANNOT BE MUTATION-PROVED: tests/rejection/if-node-is-not-implemented.sysml rejects because IfNode is absent, so no mutation of the guard path makes it leak -- the three `must be a suffix` files and the UsageBody file were proved (6 and 1 leaked respectively).\n\nEARLIER, 2026-09-19 (history). ControlNode is DONE (MergeNode, DecisionNode, JoinNode, ForkNode via ActionNodeMember, SysML 8.2.2.17.3): 193 to 200 of 554 units, 51 to 54 of 253 .sysml, exactly the three files predicted (ControlNodeTest, Fork Join Example, Merge Example) -- the sixth prediction in a row to hold. ControlNodePrefix is read but UNMARKED (UsageExtensionKeyword). tests/rejection/action-body-control-flow-is-not-implemented.sysml now rejects on `then accept s;` (AcceptNode). NEXT, two one-file levers measured over the same 13 control-node files: training/17. Control/Decision Example.sysml fails ONLY on two `if <expr> then X;` lines after `then decide;` -- GuardedTargetSuccession, the second ActionTargetSuccession alternative (8.2.2.17.8), inside the target loop that already runs after an ActionNodeMember; and training/19. Terminate Actions/Terminate Actions Example-1.sysml fails ONLY on `then terminate;` and `action stop terminate;` -- TerminateNode (8.2.2.17.4). Re-take the prediction over the WHOLE corpus for whichever is chosen before coding (attribute every diagnostic line, as predict2.py did). Note for the method: macOS has no `timeout`; bound a mutated sweep with `perl -e 'alarm 120; exec @ARGV'`, because a recogniser that looks past a token its reader does not consume makes body_elements spin without progress.\n\nEARLIER, 2026-09-19 (history). ActionBodyItem's THIRD alternative is DONE over the behaviour usages that exist (SourceSuccessionMember, SourceSuccession, SourceEnd now fully read): 190 to 193 of 554 units, 50 to 51 of 253 .sysml, exactly the one file predicted (Action Shorthand Example). A calculation body no longer ends its items at `action a;` (at_result_expression). NEXT: the CONTROL NODES, ActionNodeMember over ControlNode = MergeNode | DecisionNode | JoinNode | ForkNode (SysML 8.2.2.17.3), each `ControlNodePrefix 'merge'|'decide'|'join'|'fork' UsageDeclaration ActionBody`; ControlNodePrefix carries deviation work (SYSML21-400, see deviations.json) -- read the derived unit first. ActionBehaviorMember = BehaviorUsageMember | ActionNodeMember, so the member, the `then` prefix and the target loop are ALREADY built: add the node class to membership/behaviour_targets rather than a new dispatch. The lever: training/17. Control/Merge Example.sysml fails on ONE line, `then merge continue;`; Fork Join Example on `then fork;` and `join joinNode;`; Decision Example and ControlNodeTest are close. PREDICT PER FILE FIRST with the method that has now held five times (predict2.py in the aabe48d/90ec097 commit messages' description: copy files with `grep -rl --null | tar`, parse copies with target/debug/sv2, attribute every diagnostic line). tests/rejection/action-body-control-flow-is-not-implemented.sysml rejects on `then merge m;` and will need moving AHEAD of that commit, as 7c4d1d4 did.\n\nLATEST, 2026-09-19 afternoon (history). FlowUsage is DONE (the author chose it over finishing this step): 177 to 190 of 554 units, 46 to 50 of 253 .sysml, exactly the four files predicted. THAT CHANGES THIS STEP'S PAYOFF: vendor/corpus/sysml/src/training/14. Action Definitions/Action Shorthand Example.sysml now fails on ONE line, `then action shoot: Shoot {`, which is SourceSuccessionMember + BehaviorUsageMember -- the rest of the third ActionBodyItem alternative below. Re-take the prediction with the method below before writing code; one file is the floor, not a promise. Also untested by mutation: tests/rejection/flow-end-needs-its-feature.sysml is rejected by rule but no mutation has shown it can fail. And SuccessionFlowUsage (`succession flow`, 8.2.2.16) shares FlowDeclaration and is now a small step.\n\nSTATUS, earlier on 2026-09-19. The SETTLE FIRST question below is ANSWERED AND DONE: usages in definition, requirement, action and calculation bodies were all built as DefinitionMember, which owns definitions only (8.2.2.6.1); they now get NonOccurrenceUsageMember / OccurrenceUsageMember / StructureUsageMember / BehaviorUsageMember by 8.2.2.6.4's classes (no snapshot moved; coverage 173 to 177). So `action a;` in an action body IS now a BehaviorUsageMember, and what remains of this step is only the `SourceSuccessionMember?` prefix and the `ActionTargetSuccessionMember*` loop after it. WIDER THAN WRITTEN BELOW: SourceSuccessionMember also prefixes OccurrenceUsageMember in DefinitionBodyItem and StructureUsageMember in NonBehaviorBodyItem (8.2.2.6.1, 8.2.2.17.1), so `then part p;` in a part def is the same production. THE PREDICTION IS TAKEN, and it is ZERO even as an upper bound: over every corpus file with a line-leading `then`, no file's diagnostics are all `then <usage keyword>` or bare `then Y;` lines (55 are `then action`, 0 `then perform`). The nearest misses point elsewhere: training/14 'Action Shorthand Example' fails on ONE line, `flow from focus.image to shoot.image;` (FlowUsage), and `then event` is written 45 times (EventOccurrenceUsage behind a SourceSuccessionMember). The author was asked to choose between finishing this alternative for completeness and FlowUsage for acceptance.\n\nIMPLEMENT ActionBodyItem's THIRD alternative, restricted to the behaviours this parser already reads:\n\n    ActionBodyItem = ... | ( SourceSuccessionMember )? ActionBehaviorMember ( ActionTargetSuccessionMember )*   (SysML 8.2.2.17.1)\n    SourceSuccessionMember : FeatureMembership = 'then' SourceSuccession                  (8.2.2.9.3)\n    SourceSuccession : SuccessionAsUsage = SourceEndMember                                (8.2.2.9.3)\n    ActionBehaviorMember = BehaviorUsageMember | ActionNodeMember                         (8.2.2.17.1)\n    BehaviorUsageMember : FeatureMembership = MemberPrefix BehaviorUsageElement           (8.2.2.6.1)\n\nso that `then action a;`, `then perform p;`, and `action a; then b;` parse. BehaviorUsageElement has sixteen alternatives and only ActionUsage and PerformActionUsage exist, so BehaviorUsageElement and ActionBehaviorMember stay UNMARKED; BehaviorUsageMember, SourceSuccessionMember and SourceSuccession can be marked (their own bodies are read in full -- the convention DefinitionBody and ConnectorEndMember follow). Everything below is already built: source_end_member (empty SourceEnd), action_target_succession_member and at_action_target_succession_member (aabe48d); only the dispatch, the two new members, and the loop after the behaviour member are new. Mirror initial_node_item: one method reads the whole alternative, and the ActionTargetSuccessionMember* loop runs after the behaviour member, never as a free-standing arm of body_elements.\n\nSETTLE FIRST: today `action a;` inside an action body is built by `membership` as a DefinitionMember (Body::member maps Action to DefinitionMember). By 8.2.2.17.1 an action usage there is an ActionBehaviorMember -> BehaviorUsageMember, which is NOT in NonBehaviorBodyItem. Check which node the grammar gives it, and whether changing it moves any of the 8 snapshots (review one at a time with a reason; if one moves and you cannot name the change, stop). Also `then` + `[`: SourceEnd's OwnedMultiplicity is written after this `then` (`then [1] action a;`), unlike TargetSuccession's where it would come before; decide whether to read it here, and mark SourceEnd only if every alternative is then read.\n\nTHE MEASUREMENT, taken 2026-09-19 over every corpus file with a line-leading `then`: of 150 BARE-NAME `then Y;` lines (33 files), 67 follow a line opening on `accept`, 24 `do`, 3 `entry` and 4 `state` -- state-machine transitions, a different production entirely -- and only 3 follow an `action` usage, so `action a; then b;` alone buys little. The lever is the KEYWORD form: 98 line-leading `then` open on a keyword (`then action`, `then merge`, `then decide`, `then fork` ...). Of those, only `then action` and `then perform` are in reach; `merge`/`decide`/`fork`/`join`/`accept`/`send`/`terminate` are ActionNodes, all unimplemented. Count `then action` and `then perform` separately before claiming the 98.\n\nPREDICT THE CORPUS GAIN BEFORE WRITING THE CODE, per file, with the method used for aabe48d: copy the candidate files to the scratchpad with `grep -rl --null ... | tar --null -T - -cf - | tar -xf - -C <scratch>` (grep here is ugrep: `-Z` is NOT the NUL separator, `--null` is; and the PreToolUse hook blocks `sv2 parse` naming vendor/ directly), run `target/debug/sv2 parse` on the copies, and count files whose EVERY diagnostic line is a form this change reads. Three predictions of zero in a row have held (CalculationDefinition, InitialNodeMember, ActionTargetSuccessionMember). Do not assume this one is zero either.\n\nCOUNTING, two traps met before: a name pattern must admit NO SPACES -- `([A-Za-z_][A-Za-z0-9_]*|'[^']*')` joined by `::` -- because a pattern allowing spaces made `first a then b;` count as `first a;` this session and put the figure at 68 where it was 16. And the PreToolUse hook reads a shell loop over vendor/ as a write: use one plain grep, or `grep -rl --null | xargs -0` (ugrep: `--null`, not `-Z`), not `while read`. And a jq over several names under .claude/state/grammar/ must be ONE jq with the names in an array, not a loop.",
    "pending_decisions": [
      {
        "id": "crate-split",
        "question": "Boundary between sv2-hir and sv2-resolve is provisional. Revisit once implied specialization injection exists and its real dependencies are visible.",
        "blocks": []
      },
      {
        "id": "wiki-reproducibility",
        "question": "RESOLVED as far as it can be on one machine. The wikis are pinned by receipt: 1651 atoms in .claude/state/wiki-receipts.json, each with its clause, page, the wiki's region_sha256 to the source PDF span, and a file hash. Drift now fails the gate, and a citation naming a receipt that was never pinned now fails scripts/check_citations.py. NOT resolved: the wikis are not in the repository, because their 1651 atoms carry ~2.3 MB of verbatim OMG specification prose with no license notice, and this repository is MIT and public. So a fresh clone can verify nothing until it builds its own wikis from the specification PDFs, which are not redistributable either. Revisit only if OMG's licensing changes, or if the wiki builder can be pinned and rerun from locally-held PDFs as part of a bootstrap.",
        "blocks": [
          "verifying any clause citation on a machine that has not built the wikis"
        ]
      },
      {
        "id": "state-commits-fixed-point",
        "question": "RESOLVED. regen_state.py recorded `git log --oneline -8` in the generated block and --check compared it, which has no fixed point: the commit carrying state.json cannot record its own hash, and amending only moves it. The gate was therefore red after every commit, for ever. Fixed by naming the compared measurements explicitly \u2014 COMPARED = (coverage, tests, pins) \u2014 and a pure is_stale() over them, with four tests including the one that says a new commit is not staleness. `commits` is still measured and written for whoever reads the block at session start; it is simply not evidence. Nothing else read it: not state_report.py, not the schema's required list. NOT weakened: coverage, tests and pins are still compared exactly as before, so an un-regenerated block after real work still fails.",
        "blocks": []
      },
      {
        "id": "expression-layer-approach",
        "question": "RESOLVED: the derive-grammar six-phase pipeline. It runs, and the expression layer is being derived through it starting from KerML 8.2.5.8.1. Two pipeline defects had to be fixed first (plan enumerated the Xtext inventory; freeze check went red on the first derived unit); both are fixed and tested.",
        "blocks": []
      },
      {
        "id": "derivation-evidence-in-repo",
        "question": "RESOLVED. A unit no longer embeds its clause: it keeps the citation and a sha256 of the clause text, and grammar_next.py joins the text from the local export at read time. The units are therefore committed, so the derived rules, decisions and evidence survive a clone and are reviewable in a diff, while no specification prose enters the repository. check_derivation_text.py holds the line on every gate. NOT resolved by this: a fresh clone still cannot re-derive or verify a clause without building the wikis from locally-held PDFs, which is the standing wiki-reproducibility limit.",
        "blocks": []
      },
      {
        "id": "ADR-0016",
        "question": "Proposed: stable element identity via petname IDs carried in inline textual notes. Would answer ADR-0009; both are open in decisions.json.",
        "blocks": [
          "stable element handles in sv2-resolve"
        ],
        "adr": "docs/adr/0016-element-identity-via-petname-notes.md"
      },
      {
        "id": "ADR-0017",
        "question": "Proposed: view-scoped JSON Lines sidecar for diagram layout and styling.",
        "blocks": [
          "sidecar schema"
        ],
        "adr": "docs/adr/0017-view-scoped-json-lines-sidecar-for-diagram-layout-and-styling.md"
      },
      {
        "id": "bracket-role",
        "question": "RESOLVED: positional, and the position cannot be reached from both sides. A '[' reached from UsageDeclaration's FeatureSpecializationPart is a MultiplicityRange (SysML 8.2.2.6.6); a '[' reached after a primary expression operand is a BracketExpression (KerML 8.2.5.8.2). The hypothesis held for every corpus shape. The decisive line is `attribute mass : MassValue[1] = 1200 [kg];`, which writes BOTH roles and parses: multiplicity_part is reachable only from feature_specialization_part, which sits inside UsageDeclaration, and a UsageDeclaration is finished before the ValuePart's '=' is read, so the two can never contend for one token. What separates the productions themselves is the OPERAND: a BracketExpression has one before the '[' and a MultiplicityRange has none, which is what tests/rejection/bracket-expression-needs-an-operand.sysml holds. The corpus also writes a bracketed name (`[one]`, `[spatialCF]`, `[source]`) that is indistinguishable from a multiplicity bound by CONTENT, which is why content was never a candidate rule. Both roles are implemented and 297 corpus files are no longer blocked on this; acceptance went 32 to 39 .sysml when BracketExpression landed.",
        "blocks": []
      },
      {
        "id": "max-depth",
        "question": "Is MAX_DEPTH = 400 in crates/sv2-syntax/src/parser.rs the right recursion limit, and should it be configurable? It was introduced because the recursive expression parser OVERFLOWED THE STACK, which aborts the process and so is strictly worse than the panic invariant 3 forbids. Measured thresholds before the guard: unary operators between 2000 and 5000 levels, parenthesised expressions and nested bodies between 5000 and 10000. 400 is far past anything a person writes, but it is a number nobody derived from a source, and it changes what the parser accepts.\n\nWIDENED by the postfix expressions. DEPTH IS A PROPERTY OF THE TREE, NOT OF THE PARSER'S RECURSION, and the feature chain proved it: `postfix_tail` is a LOOP and uses no stack at all, yet a 50000-link chain aborted a test thread, because every link wraps the last and the tree is as deep as the chain is long. Folding also stopped being linear at that size. Chains and brackets are therefore counted against the same budget as nested bodies. Anything else that folds -- IndexExpression, FunctionOperationExpression, CollectExpression, SelectExpression, all of which join the same loop -- must count too, and the loop is the place to enforce it once.",
        "blocks": [
          "nothing today; revisit if a generated model is ever rejected for depth"
        ]
      },
      {
        "id": "rejection-provenance",
        "question": "PARTLY RESOLVED, and the mechanism is in: a rejection file declares its reason in its header as `// rejects: <CODE> on: <source line>`, and scripts/corpus_sweep.py checks that declaration against the first diagnostic the parser actually raises -- comparing the line's TEXT, not its number, because headers are edited often and body lines are not. Three ways it fails: no declaration and not listed as pending, a declaration the parser contradicts, and a file that declares while still listed as pending. Ten pytest cases, and both live failures were proved by mutation. NOT resolved: 117 of 135 cases predate the check and are listed in tests/rejection-provenance-pending.txt, which may only shrink. Backfilling is authoring work -- each declaration must come from the rule the file states, never from what the parser printed -- so it is done a few at a time, and every NEW case declares from the start.",
        "blocks": []
      },
      {
        "id": "coverage-marker-audit",
        "question": "Is there another production marked `// production: X` whose function does not read all of X's body? This decision was opened when four reference productions were found marked while the OwnedFeatureChain alternative of SysML 8.2.2.6.5 was absent, and a reviewer then found a fifth of the same shape (OwnedFeatureTyping). All five are fixed and share one `chainable_target`. The alternation that had five instances is now exhaustively checked; NO OTHER SHAPE IS, so the question stands for the rest of the grammar.\n\nAPPLIED PROSPECTIVELY FOR THE FIRST TIME this session, which is the cheapest form of the fix and needs no gate check: InstantiatedTypeMember (KerML 8.2.5.8.3) is `InstantiatedTypeReference | OwnedFeatureChainMember`, the invocation reads only the first alternative, and it was left UNMARKED while the eight productions around it were marked. The node is built; the alternation is not claimed. Doing that at the time costs nothing, and it is what the four-then-five defects cost when it was not done.\n\nA false `implemented` is still the one coverage error that cannot be found by reading the report, because the report is what is wrong. bnf_coverage.py counts markers; it cannot compare a function against the rule body it claims. Candidate check, unchanged: for each marked production, assert that every literal keyword in its derived rule appears in the marked function and that every `alt` arm is reachable. Weak, but it would have caught all five.",
        "blocks": [
          "trusting the coverage percentage as a measure of what parses"
        ]
      },
      {
        "id": "comment-claim-checking",
        "question": "Nothing in the gate compares a CITED CLAUSE against what the comment claims of it, and this session produced SIX instances across three review passes -- enough to call it the repository's characteristic defect rather than a run of bad luck. They split into two kinds.\n\nWRONG CLAUSE NUMBER, or a right number carrying a false argument:\n  (1) three comments cited KerML 8.2.2.6 for `ref`, `action` and `constraint` being reserved; that clause lists KerML's OWN reserved words and contains none of the three. SysML 8.2.2.1.2 is the clause that reserves them.\n  (2) `calculation_definition` said CalculationDefinition is NOT an OccurrenceDefinition 'unlike the sibling it shares a body with'. It IS one, via ActionDefinition > Behavior, where ConstraintDefinition reaches it via Predicate. The CONCLUSION was right and the ARGUMENT was false, which is the shape nobody rereads.\n  (3) and (4) `definition_element` and `usage_element` cited SysML 8.2.2.6.1 for the DefinitionElement/UsageElement alternation; both are stated at 8.2.2.5.2, and 8.2.2.6.1 merely USES DefinitionElement inside DefinitionMember.\n\nMISQUOTED METACLASS under a correct clause number -- a new sub-kind, found in the invocation commit and the cheapest of all to check mechanically:\n  (5) `InstantiatedTypeReference : Feature` where the clause and the pinned inventory both say `Type`.\n  (6) `NamedArgumentMember : ParameterMembership` where both say `FeatureMembership`. That one also hid a real fact: a named argument's membership is NOT the ParameterMembership its positional sibling ArgumentMember is, and the wrong transcription erased the distinction it should have drawn.\n\nAll six are fixed. Every one passed the gate.\n\nTHE SUB-KIND IS MECHANICALLY CHECKABLE TODAY and the check is small: every marked production's comment writes `Name : Metaclass = body`, and .claude/state/grammar/bnf-productions.json carries `metaclass` for each rule. Comparing the two is a string equality over the productions already marked -- it needs no wiki, so it runs on a fresh clone, which is the standing limit that blocks the harder version. That would have caught (5) and (6) and nothing else. The wider check -- retrieve each cited clause and assert the production names in the comment appear in it -- would have caught (1), (3) and (4), and needs the wikis. (2) is a claim about a specialization chain and would need index/specialization-tree.json.\n\ncheck_citations.py only verifies that a named receipt was pinned; it never opens the clause. Same family as [rejection-provenance] and [coverage-marker-audit]: a claim that still passes after the thing it claimed stopped being true. Of the three, THIS one now has the cheapest first increment, and that increment is the recommended next move on all three.\n\nFOUR MORE INSTANCES on 2026-09-19, all fixed in 752ff41, two of them pre-existing and two of them mine:\n  (7) `admits_return_parameter` said ActionBodyItem is reached by RequirementBodyItem and DefinitionBodyItem. NEITHER reaches it (8.2.2.21.1, 8.2.2.6.1). It was a REACHABILITY claim, a new sub-kind, and it was dangerous rather than cosmetic: the next_step told the session to decide exactly that question, and widening the new predicate on the comment's word would have admitted `first` in a requirement body.\n  (8) `admits_requirement_constraint` cited CaseBodyItem as 8.2.2.23; it is 8.2.2.22. A wrong clause NUMBER, the first kind.\n  (9) mine, caught by the reviewer: 'ActionBodyItem is reached by ActionBody, CalculationBodyItem and CaseBodyItem, and by nothing else' -- it is EIGHT (add ActionBodyParameter 8.2.2.17.7 and the four Transition*ActionUsage bodies 8.2.2.18.3). The search that would have shown it had been run and its result summarised wrongly.\n  (10) mine, caught by the reviewer: a rejection header gave the error position as 21:2. True when measured; moved to 23:2 by the header edit that wrote it. Positions are no longer written in rejection headers.\nAlso caught before landing, by checking while writing the tests: two corpus claims (13 for 15; the wrong file for `private first A3;`).\n\nREACHABILITY is now as checkable as the metaclass sub-kind, and needs no wiki: `jq '.rules[] | select(.body | test(\"\\\\bActionBodyItem\\\\b\")) | .name'` over bnf-productions.json is the whole of (7) and (9). A check that extracts every 'X is reached by A, B and C' claim is too loose to write; asserting the answer inside the Body predicate's own test -- one case per reacher that has a Body variant -- is not.",
        "blocks": [
          "trusting any clause citation in the codebase without re-retrieving it"
        ]
      },
      {
        "id": "wiki-receipts-vacuous",
        "question": "RESOLVED on 2026-09-19, with one standing caveat. LATER THE SAME DAY: the next session started with the image mounted but BOTH variables unset, so the check was inert again -- and ~/.zprofile had a typo, exporting SV2_SYSML_WIKI=/Volumes/sv2-wikis/sysml-v20llm-wiki/wiki, a path that does not exist; fixed to sysml-v2-llm-wiki. With the variables exported by hand the check reported 'wiki receipts current (1651 atoms)' and citations 189 receipts, all pinned. A session started from a non-login shell will still not inherit them, which is the skip-versus-pass point below in practice. The original zips, which store both spellings as separate entries, were unpacked onto a CASE-SENSITIVE sparse image (~/sv2-wikis.sparseimage, mounted at /Volumes/sv2-wikis) at /Volumes/sv2-wikis/{sysml-v2-llm-wiki,kerml-llm-wiki}/wiki, and with SV2_SYSML_WIKI / SV2_KERML_WIKI pointed there `build_wiki_receipts.py --check` reports 'wiki receipts current (1651 atoms)' -- the prediction below held exactly, and NOTHING WAS RE-PINNED. The full gate is green with the wikis visible, and check_citations.py reports 189 receipts across 317 deviations, all pinned. The author's ~/.zprofile attaches the image and exports the two variables; hooks inherit them only from a session started after that. CAVEAT, still open: when the image is not mounted or the variables are unset, the check is inert again and the gate still prints `pass` -- the skip-versus-pass point at the end of this entry stands. HISTORY: The `wiki receipts` gate check had been passing on this machine WITHOUT CHECKING ANYTHING. build_wiki_receipts.py --check exits 0 with 'wiki not found: sysml, kerml -- receipts not rebuilt' when the wikis are not at the default paths (~/.sysml-v2-llm-wiki/wiki, ~/.kerml-llm-wiki/wiki) and SV2_SYSML_WIKI / SV2_KERML_WIKI are unset -- which is the case here. The wikis on this machine are at ~/projects/deal-lang/sysml-v2-wiki and ~/projects/deal-lang/kerml-wiki. Pointed at them, the check is RED: 'wiki receipts are stale'. So EITHER those copies are not the wikis the 1651 receipts were pinned from, OR the manifest has drifted from them; which one is unknown. NOT done, deliberately: rebuilding the receipts from those copies would re-pin every citation in the repository against a source nobody has compared with the pinned one, which is fixing the check. Individually, the two receipts cited on 2026-09-19 (4e3ffb79 ActionDefinitions 8.2.2.17.1, 339ef468 Succession-Shorthands 7.17.4) were grepped and ARE pinned. FOUND, same day: the author copied the wikis from the AWS Workspace (where the receipts were built) to ~/.claude/wikis/{sysml-v2-llm-wiki,kerml-llm-wiki}/wiki, and a record-by-record comparison against the pinned manifest -- read-only, the manifest built in memory and never written -- gave 1651 of 1651 atoms by id, 1650 BYTE-IDENTICAL, and ONE different: sysml MeasurementReferences::NullTransformation. Cause: the pinned wiki holds two DISTINCT atoms whose files differ only by case, atoms/library/MeasurementReferences__NullTransformation.md (pdf 626) and MeasurementReferences__nullTransformation.md (pdf 627) -- the only such pair of the 1651 -- and on this Mac's case-insensitive disk only the second survived the copy, so the builder reads it for both. The same class of defect as [case-only-renames], outside the repository this time. So these ARE the pinned wikis; the deal-lang copies are an older build (1650 atoms, the lowercase atom absent, 31 atoms with identical region_sha256 but different file bytes). Nothing in the repository cites either atom. DO NOT regenerate the manifest from a copy on a case-insensitive disk: it would pin the wrong content for NullTransformation and lose the page-626 atom. And do not export SV2_SYSML_WIKI at a case-insensitive copy: the check would then be red on this Mac permanently. The fix is to re-copy both wikis FROM THE WORKSPACE onto a case-sensitive volume (e.g. a sparse image, `hdiutil create -type SPARSE -fs 'Case-sensitive APFS' -volname sv2-wikis`), point SV2_SYSML_WIKI / SV2_KERML_WIKI at it, and confirm `build_wiki_receipts.py --check` reports current -- predicted, not yet observed. Worth doing alongside: have build_wiki_receipts.py name a case collision among atom paths when it finds one, rather than reporting the whole manifest 'stale', which cost an investigation to decode. Separately, 'inert when absent' is a correct policy for a fresh clone and a misleading one here: a PASS line that checked nothing reads the same as one that did. Worth considering whether the gate should print `skip` rather than `pass` when the wikis are absent, as it already does for shfmt and ruff.",
        "blocks": [
          "trusting the `wiki receipts` pass line in a session where /Volumes/sv2-wikis is not mounted"
        ]
      },
      {
        "id": "coverage-name-collision",
        "question": "RESOLVED on 2026-09-19 by the author's decision ('I would rather get an honest percentage'). bnf_coverage.py now counts ADR-0015's LIVE GRAMMAR UNITS -- 554, read from .claude/state/grammar/units/ -- instead of the inventory's 558 names. The name count was wrong both ways: it collapsed the 28 productions the two languages state differently into one each, so a SysML marker claimed KerML's different production too (MultiplicityRange: SysML `[1..*]` against KerML's `multiplicity` declaration), and it counted 32 lexical terminals no marker can claim. A marker on a split production must now say `@kerml` or `@sysml`; a bare one, or a scope with no live unit, is `absent` and fails the gate, each with its own message. Fifteen existing markers were bare on split names: thirteen read SysML's body only and became `@sysml`; RootNamespace and PackageBody read both through Body::Root / Body::Package, are exercised in tests/kerml.rs, and carry both markers. Result: 171 of 558 became 173 of 554 (30.6% to 31.2%). Up, not down: the thirteen count one unit each where they counted one name, the two dual markers count two units each, and the 32 names that left the denominator are all lexical terminals (ALPHABETIC_CHARACTER ... WHITE_SPACE, TYPED_BY, REFERENCES and the like), none of which was ever claimable. NOT AUDITED: whether each of the other 156 markers reads its unit's whole body; only the fifteen split ones were checked against both bodies here. THE PREMISE OF THIS ENTRY WAS PARTLY WRONG: ConnectorEndMember and ConnectorEnd are stated alike in both languages and are ONE shared unit each, so marking them never claimed a different KerML production; the parser comment saying so was corrected. Tests in scripts/tests/test_bnf_coverage.py; the add-production skill, docs/DERIVATION.md and docs/CRATES.md updated.",
        "blocks": []
      },
      {
        "id": "case-only-renames",
        "question": "RESOLVED AS FAR AS THE REPOSITORY GOES; the generator is not fixed. From 3eaa618 the index tracked two retired grammar units beside live siblings whose names differed only by case (MetaClassificationTestOperator / MetaclassificationTestOperator, Ownedsubclassification / OwnedSubclassification). On a case-insensitive disk each pair is one file; a checkout on 2026-09-19 left the RETIRED content on disk and the frozen-grammar check went red with a hash no commit produced. b3ae9e7 untracked the two retired spellings and added scripts/check_path_case.py to the gate, which fails on any two tracked paths (files or directories) that fold together under casefold and NFC; it reports exactly those two pairs against the parent commit. Cost accepted: grammar_rebase.py no longer sees those two as retired, so a future rebase will not list them as removed. OPEN: grammar_plan.py still retires a unit and creates its successor under whatever spelling the inventory gives, so a case-only rename upstream will produce the pair again -- the gate now catches it the moment it is committed, but the generator could refuse instead, or delete the retired file when its successor folds to the same name.",
        "blocks": []
      },
      {
        "id": "script-tests-skipped",
        "question": "RESOLVED on 2026-09-19. The gate's `script tests` step had been SKIPPED on this machine ('pytest not installed for python3.12', and under 3.11 before it), so no Python test suite ran in any gate, and validate_state.py only ever took its structural fallback. Homebrew's python3.12 is externally managed and refuses a pip install, so pytest and jsonschema are now a uv `dev` dependency group -- outside `[project]`, which keeps STD-001-PY \u00a712's 'three keys and no dependencies' -- and gate.sh runs the script tests and validate_state.py under .venv/bin/python when it exists. Every check script still runs under the system python3.12, standard library only. First run: 454 passed, 1 skipped, including test_check_path_case.py and the test files the 3.12 move edited; the state schema check is now the full jsonschema one (1129 units). With .venv moved aside the gate falls back exactly as before -- tests skipped with a `uv sync` hint, schema structural -- and stays green, so it still runs where nothing is installed (STD-001-PY 2.2.0 \u00a73.1).",
        "blocks": []
      },
      {
        "id": "progress-guard-masks-mismatch",
        "question": "body_elements now takes one token through error_token when a pass consumes nothing (ac031cf). That bounds what a recogniser/production mismatch costs, but it also HIDES the defect that used to announce itself as a hang: `snapshot first a then b;` with the old recogniser would now yield PARSE-EXPECTED then PARSE-UNEXPECTED, and a unit test that only calls parse_rejected (tests/parser.rs:2477) passes either way. A rejection FILE still catches it, because its `rejects:` line names the first code (PARSE-UNEXPECTED). Should a guard firing be observable -- a debug-only counter on the parser, or a distinct DiagnosticCode such as PARSE-NO-PROGRESS -- so the corpus sweep and the unit tests can assert it is zero?",
        "blocks": [
          "nothing today; a future recogniser/production mismatch reaches a commit as a wrong diagnostic rather than a hang"
        ]
      },
      {
        "id": "recogniser-lists",
        "question": "at_sysml_keyword_member is now the one list both at_member_element and at_result_expression ask (a8cb979), after four productions were added to one and not the other. Three lists are still kept by hand and each new usage has to be added to all of them: at_member_keyword (recovery's stopping set), at_source_succession_member (the occurrence/behaviour usages a `then` may precede) and usage_element_of_class (the dispatch). Should at_member_keyword ask at_sysml_keyword_member, and at_source_succession_member ask a shared 'occurrence usage' recogniser, so a new production cannot be half-wired? Note that at_member_keyword's gaps also hide diagnostics: recovery from an unrecognised construct runs past the statements behind it (df81503/3446ff3). UPDATED 2026-09-22: the state layer added four more hand edits of the same kind -- at_state_usage and at_exhibit_state_usage to at_sysml_keyword_member, at_member_keyword, usage_element_of_class and at_source_succession_member each, and `transition`, `entry`, `do` and `exit` to at_member_keyword -- and body_specific_item now holds seven keyword-led body items that are in NO shared list. The cost is growing with each behaviour usage.",
        "blocks": [
          "nothing today; every new usage production pays the cost and risks a half-wired recogniser"
        ]
      },
      {
        "id": "multiplicity-only-declaration",
        "question": "FeatureSpecializationPart's second alternative opens on a MultiplicityPart (`[1..3]`, SysML 8.2.2.6.5), and neither at_default_reference_usage nor at_enumerated_value looks for a `[`, so a usage declared as a multiplicity alone is not recognised by either. Raised by the reviewer of 39b0eef as pre-existing. Is any corpus form affected, and should both recognisers admit `[`?",
        "blocks": [
          "nothing known; no corpus file is known to write it"
        ]
      },
      {
        "id": "variant-reference-multiplicity",
        "question": "7.6.7 (receipt 5a7843af) says a variant written as a reference to a separately declared usage \"may also optionally further constrain the variant usage by including a multiplicity and/or further specializations\", but VariantReference (SysML 8.2.2.6.3) is `OwnedReferenceSubsetting FeatureSpecialization* UsageBody` and writes no MultiplicityPart. The parser follows the grammar and `variant x[1];` is reported (a_variant_usage_member_is_bounded_by_its_rules holds it), but the conflict is recorded only in a comment at `variant_reference`, not in deviations.json. No corpus file writes the form. Run the grammar-adjudicator and record the outcome, checking the Pilot's VariantReference and the OMG issue list.",
        "blocks": [
          "nothing today; the prose form is rejected and no corpus file writes it"
        ]
      },
      {
        "id": "keyword-table-per-language",
        "question": "The parser's keyword table (KEYWORDS, generated from .claude/state/grammar/keywords.json) is ONE table for both languages, so every SysML reserved word is also treated as reserved in a .kerml file, and vice versa: `part` and `classifier` are in the same list. The pinned sources disagree: KerML's RESERVED_KEYWORD (vendor/spec-bnf/KerML-textual-bnf.kebnf, KerML 8.2.2.6) omits SysML-only words, and omits `new` although KerML 8.2.5.8.3's ConstructorExpression writes it as a literal, while SysML's Tier B' list (SysML-textual-bnf.kebnf:20) includes `new` and its clause list in the wiki does not. So `feature new;` or `feature part;` is rejected in KerML where its own list admits it. Found by the conformance review of the send/assign/`new` construct (2026-09-22); it predates that change (KwNew was already in the table). Decide: split the table per Language (ADR-0014 already makes the grammars two), and whether `new` in KerML is a deviation in the SYSML21-11 family (missing reserved keywords) or simply not reserved. Needs the grammar-adjudicator. ADDED 2026-09-23 (80415c1, where it was first filed as a duplicate, reserved-words-per-language, now folded in here): the effect in SysML is concrete and spec-visible. The 44 KerML-only words -- assoc, behavior, bool, chains, class, classifier, composite, conjugate, conjugates, conjugation, connector, const, datatype, differences, disjoining, disjoint, expr, feature, featured, featuring, function, interaction, intersects, inv, inverse, inverting, member, metaclass, multiplicity, namespace, portion, predicate, redefinition, specialization, step, struct, subclassifier, subset, subtype, type, typed, typing, unions, var -- are refused as SysML names (`part step;` is rejected), and SysML 7.17.12's first while-loop example names its body clause `step`; the_while_until_example_of_7_17_12_parses writes it `'step'` until this is settled. The reverse set is 72 SysML-only words refused in .kerml files. The fix is in scripts/gen_syntax_kinds.py (kinds.rs is machine-owned): per-language keyword sets, asked by is_name with the parse's Language.",
        "blocks": [
          "a KerML name spelled like a SysML-only keyword"
        ]
      },
      {
        "id": "reserved-word-result-expression",
        "question": "A calculation, constraint or case body hands any reserved word it has no item for (`require`, `verify` in a case body, `stakeholder`, `frame`) to the result-expression reader, which reports 'expected an expression' and then an 'unexpected `}`' as the body closes early. Still rejected, but the diagnostic is misleading and the second one spurious. at_result_expression lists the reserved ITEM words (variant, subject, actor, objective) by hand. The general fix is to derive which reserved words can BEGIN an OwnedExpression (KerML 8.2.5.8: if, not, all, true, false, null, new, istype, hastype, as, meta, @, @@ ...) and treat every other reserved word as not-an-expression. Needs the derivation checked against the expression units, not a hand list.",
        "blocks": []
      },
      {
        "id": "feature-specialization-multiplicity-gap",
        "question": "RESOLVED on 2026-09-23. FeatureSpecializationPart may open on a MultiplicityPart (SysML 8.2.2.6.5), and PerformActionUsageDeclaration, ExhibitStateUsage, AssertConstraintUsage and RequirementConstraintUsage asked only at_feature_specialization, so `perform x[1];`, `exhibit s[1];`, `assert c[1] {}` and `require c[1] {}` were rejected. The corpus did write one after all: examples/Simple Tests/RequirementTest.sysml:34-35, `assume c1 [0..*];` and `require c2 [0..*];`, reached once `concern` was read. Settled by one helper, optional_feature_specialization_part, which every one of the twelve `FeatureSpecializationPart?` sites now calls, so no production can ask half the question again; tests a_reference_may_be_followed_by_a_multiplicity and a_reference_takes_one_multiplicity hold it.",
        "blocks": []
      },
      {
        "id": "adr-0015-kernel-claim",
        "question": "ADR-0015's context says SysML's AnnotatingElement taken literally opens KerML's MetadataBody and the whole kernel; the grammar-adjudicator's recomputation of SysML's reach with _grammar.py (2026-09-22) shows it reaches SysML's own MetadataBody and PrefixMetadataMember, the one KerML-only production being MetadataFeatureDeclaration, which prints MetadataUsageDeclaration's text. The deviations.json entry was corrected; amending an accepted ADR's context is the owner's call.",
        "blocks": []
      },
      {
        "id": "shared-unit-kerml-prefix-gap",
        "question": "Package and LibraryPackage are each ONE shared unit (ADR-0015) and both are marked, while their PrefixMetadataMember* is read in SysML only: KerML's is PrefixMetadataMember@kerml over PrefixMetadataFeature (8.2.5.12), a separate unit tracked as unimplemented, so `#X package P;` and `library #X package L;` in a .kerml file are reported. The 2026-09-22 conformance review judged the markers honest because the gap is a unit of its own. Confirm that reading of the coverage model, or withhold shared markers until every language's references are read; revisit when PrefixMetadataFeature@kerml lands, which settles it either way.",
        "blocks": []
      },
      {
        "id": "allocation-arity",
        "question": "7.15.2 (receipt 546a588c) says \"Allocation definitions and usages are always binary, having exactly two end features, even if abstract\", but AllocationUsageDeclaration (SysML 8.2.2.15) writes ConnectorPart, which admits NaryConnectorPart, and the one constraint of AllocationUsage (checkAllocationUsageSpecialization, 8.3.15.3, receipt 13aa56df) says nothing of arity. The parser follows the grammar: `allocate (a, b, c);` parses (tests: an_allocation_usage_may_name_its_ends_in_parentheses), and examples/Simple Tests/AllocationTest.sysml:30 writes the parenthesised form with two ends. Where is the prose rule enforced, if anywhere: a diagnostic in sv2-resolve on an allocation with other than two ends, or nowhere until an OMG issue settles which of 7.15.2 and 8.2.2.15 is right? deviations.json does not fit: it records Pilot-versus-specification differences, and this is the specification's prose against its own grammar.",
        "blocks": [
          "nothing in sv2-syntax; the arity check, if any, when sv2-resolve exists"
        ]
      },
      {
        "id": "expression-body-semicolon",
        "question": "RESOLVED on 2026-09-23 by the author, on the recommendation below: braced only. Deviation ExpressionBody's record now says the reading is CalculationBody's braced alternative alone, with the corpus count as evidence (17 expression bodies, all braced, none a bare `;`); ExpressionBody@sysml is marked; an_expression_body_is_braced and tests/rejection/expression-body-is-braced.sysml hold `attribute x = ;;` and `a->f;` rejected. THE QUESTION WAS: Deviation ExpressionBody (follow_xtext) reads SysML's ExpressionBody as CalculationBody, which is `';' | '{' CalculationBodyPart '}'` (SysML 8.2.2.19). Taken literally the `;` alternative makes a bare `;` a BodyExpression wherever a BaseExpression or a BodyArgumentMember may stand, so `attribute x = ;;` would be an attribute valued by an empty body and `a->f;` a complete operation. The Pilot inherits exactly that (SysML.xtext:2437). The parser reaches only the braced alternative, because body_expression's callers dispatch on `{`, and ExpressionBody@sysml is therefore left UNMARKED. Recommendation: keep the braced reading and narrow the deviation's record to say so, since the corpus's evidence for the deviation (7b:127, 15_05:9) is braced bodies only and no corpus file writes a `;` body; the alternative is to read the `;` form and mark the unit. Decide before marking ExpressionBody@sysml.",
        "blocks": []
      },
      {
        "id": "state-body-terminate",
        "question": "OPEN. SysML 7.18.3's OnOff6 (Transition-Usages layer note, the example ending `action stop terminate;` inside `state def OnOff6 { ... }`) writes a TerminateNode as a STATE body item. No grammar admits it there: StateBodyItem (SysML 8.2.2.18.1) reaches usages only through BehaviorUsageMember, whose BehaviorUsageElement (8.2.2.6.4) lists no ActionNode, and the Pilot's fragment StateBodyItem (SysML.xtext:1754) is the same; ActionNodeMember belongs to ActionBehaviorMember (8.2.2.17.1), an action-body production. No corpus file writes `terminate` in a state body. The parser follows the BNF and the Pilot, so OnOff6 is rejected at `terminate`, while TerminateNode itself is read wherever an ActionBodyItem is. Needs a grammar-adjudicator pass and, if the example is the error, a deviations.json entry recording it. Not blocking: the text is rejected by rule until decided.",
        "blocks": []
      },
      {
        "id": "if-then-brace-example",
        "question": "OPEN. SysML 7.17.11's third example (If-Action-Usages layer note, receipt 5a98cecc) writes `if threat.level == high then { ... } else if threat.level == medium then { ... } else { ... }`: a `then` between the condition and the then clause. IfNode (SysML 8.2.2.17.7) writes none -- `ActionNodePrefix 'if' ExpressionParameterMember ActionBodyParameterMember ( 'else' ... )?` -- and neither does the Pilot's IfNode; no corpus file writes `then {` after an if condition (the corpus's IfNodes, StructuredControlTest.sysml:7-17 and Control Structures Example.sysml:19, write the brace directly), and the same clause's second example writes `if selectedSensor != null {` without it. The parser follows the BNF and the Pilot: the `then` makes the text a GuardedTargetSuccession, which then fails at `{`. Needs a grammar-adjudicator pass and, if the example is the error, a deviations.json entry recording it. Not blocking.",
        "blocks": []
      }
    ],
    "log": [
      {
        "summary": "Closed the SysML grammar and froze the reference. 473 to 554 of 554 live units verified, none pending and none in conflict; frozen at sha256 0b9abe64f1f3ce2b, 554 productions, ledger entry 1. The oracle went from 155 to all 311 corpus files accepted, still catching all 37 negatives with none leaked. Four filed specification defects repaired and recorded, each issue page fetched and read: SYSML21-408 and -624 (both in SendNode's single line), -400 (ControlNodePrefix) and -401 (TriggerExpression). ForVariableDeclarationMember adjudicated, which left SysML with no unreachable production. The oracle's state guard was re-derived from a measurement: ~143 items per token scaling linearly, so the 400_000 absolute cap was mis-calibrated rather than the grammar ambiguous, and it is now a per-token budget with four tests. Also declared sv2-ast, sv2-hir and sv2-resolve as stub crates, and proposed ADR-0016 and ADR-0017.",
        "date": "2026-09-17",
        "gate": "green"
      },
      {
        "date": "2026-09-17",
        "gate": "green",
        "summary": "Implemented the expression operator core: OwnedExpression by precedence climbing over the fifteen tiers of KerML 8.2.5.8.1 table 6, plus MultiplicityPart, ValuePart and ElementFilterMember, which it unblocked. Coverage 57 to 106 of 558; negative cases 49 to 65. Precedence is NOT in the grammar and cannot be -- note 2 of that clause says so -- so the parser reads docs/operator-precedence.toml, and a test diffs that file against the parser's INFIX table both ways so neither can be edited alone; deviations.json had already recorded the decision under BinaryOperatorExpression as spec_only/follow_spec. Found and fixed a defect this introduced: deep nesting overflowed the stack, which aborts rather than panics and so breaks invariant 3; added MAX_DEPTH and a round-trip test at 50000 levels for parentheses, unary operators and nested bodies. spec-conformance-reviewer returned one blocking finding, all fixed: the FeatureSpecializationPart comment quoted the Pilot's '( -> FeatureSpecialization )+' syntactic predicate as though it were KerML 8.2.4.3.1, and that '->' is in neither specification's BNF. Also committed a draft docs/CRATES.md and the author's spelling corrections to README.md and NOTICE.md."
      },
      {
        "date": "2026-09-18",
        "gate": "green",
        "summary": "Ten commits closing the requirement, constraint and action DECLARATION layers. Coverage 133 to 156 of 558; acceptance 29 to 44 .sysml; rejection corpus 77 to 97 files. Added RequirementDefinition and RequirementBody, SubjectMember, ConstraintDefinition with CalculationBody and ResultExpressionMember, RequirementConstraintMember, ActionDefinition with ActionBody, ActionUsage and PerformActionUsage; in the expression layer FeatureChainExpression and BracketExpression, which share one left-folding postfix loop; in the reference layer OwnedFeatureChain. RESOLVED [bracket-role]: the two roles of '[' are told apart by POSITION and cannot contend, because multiplicity_part is reachable only from feature_specialization_part, which is finished before the ValuePart's '=' is read; `attribute mass : MassValue[1] = 1200 [kg];` writes both and parses. FOUND AND FIXED A COVERAGE OVERCLAIM: four reference productions were marked while the OwnedFeatureChain alternative was absent; new pending decision [coverage-marker-audit] asks whether others are. MADE AN ADJUDICATION TESTABLE: the RequirementConstraintUsage conflict, resolved follow_spec in September, could not be checked until both bodies existed, and `require someRef { a <= b }` is now the rejection file that fails if the parser ever drifts to the Pilot's reading. Two defects caught by their own tests: a 50000-link feature chain aborted a test thread although the fold is a loop, because the TREE is as deep as the chain is long, and chains are now bounded by MAX_DEPTH like any nesting; and a test asserting `subject;` was rejected was wrong, because Identification is fully optional and an anonymous subject is grammatical. New pending decision [rejection-provenance] records a rejection file that went stale silently. The action layer's reading is CORRECTED: it was a 37-file lever meaning declarations, not successions -- control flow led one failing file before this work and nine after it."
      },
      {
        "date": "2026-09-18",
        "gate": "green",
        "summary": "Four commits closing the calculation layer and opening the invocation. Coverage 156 to 166 of 558; acceptance 44 to 46 .sysml and 15 to 17 .kerml, the first KerML movement since the language split; rejection corpus 97 to 103. Added CalculationDefinition, ReturnParameterMember, and the invocation family -- InvocationExpression, InstantiatedTypeReference, ArgumentList, PositionalArgumentList, NamedArgumentList, NamedArgumentMember, NamedArgument and ParameterRedefinition. LESSON (1) EARNED A FOURTH AND FIFTH TIME, and this time it was applied BEFORE the work rather than after: state.json predicted CalculationDefinition would buy three corpus files, and it bought ZERO, because all thirteen files that write `calc def` also write `return` -- measured before the code was written, and the rejection file being deleted said so in its own comment. ReturnParameterMember then bought one, not thirteen, because twelve of those files have a further blocker. The invocation bought three, two of them KerML, all three the same MassRollup model written twice. A TEST EXPECTATION WAS WRONG AND THE PARSER WAS RIGHT: `f(a, b)` builds THREE EmptyResultMembers because KerML 8.2.5.8.3 names one in FeatureReferenceExpression as well, and each argument is one; the count was corrected to what the clause states and `f()` added to isolate the invocation's own, rather than the assertion being loosened. APPLIED [coverage-marker-audit] PROSPECTIVELY for the first time: InstantiatedTypeMember was left unmarked because only one of its two alternatives is read. The spec-conformance-reviewer ran twice and found FOUR FALSE CLAIMS, all fixed: CalculationDefinition wrongly said NOT to be an OccurrenceDefinition, 8.2.2.6.1 cited for an alternation stated at 8.2.2.5.2, and then two MISQUOTED METACLASSES under correct clause numbers -- `InstantiatedTypeReference : Feature` for `Type` and `NamedArgumentMember : ParameterMembership` for `FeatureMembership`. With the three from the previous session that is six instances, and the new pending decision [comment-claim-checking] records that the metaclass sub-kind is checkable today by string equality against bnf-productions.json, with no wiki and so on a fresh clone. The invocation commit's own message also had an off-by-one -- it said acceptance went 44 to 46 .sysml when its parent already stood at 45 -- corrected in the follow-up commit rather than amended away. OPERATIONAL DEFECT, mine, fully repaired: a `git add -A` swept a 3.0 GB untracked WebStorm install into the ReturnParameterMember commit. Nothing was pushed; the commit was rebuilt by soft reset as 8565c00 with the eleven real files, verified byte-identical on the non-WebStorm paths, and the orphaned objects were reflog-expired and collected, taking .git from 1.1 GB to 3.9 MB. No branch, tag or remote ever contained them and ui/shell was never involved. Staging in this repository is by explicit path from here on."
      },
      {
        "date": "2026-09-19",
        "gate": "green",
        "summary": "Five commits: two repairing the repository, three moving the grammar. Coverage 166 to 167 of 558; acceptance unchanged at 46/253 .sysml and 17/58 .kerml, AS PREDICTED; rejection corpus 103 to 106. THE SESSION OPENED BLOCKED: the PreToolUse path guard fails closed without python3.11, which was not on PATH, so every Bash, Edit and Write was refused. Moved the repository to python3.12 (193e9f7), which STD-001-PY \u00a712 had already planned for once 3.12 was installed: the hook shims, gate.sh, corpus-sweep.sh, grammar-preflight.sh, the mypy python_version pair in pyproject.toml and STD-001-PY (2.1.0) together, and every python3.11 string; shims tested with the interpreter present and absent per STD-003-SH \u00a711. THEN THE GATE WAS RED on the frozen grammar, with a hash no commit produced. Cause, found by listing the index: since 3eaa618 two RETIRED grammar units were tracked beside live siblings whose names differ only by case, one file each on a case-insensitive disk, and a checkout had left the retired content on disk. Restoring the committed file did not help, because restoring one spelling overwrites the other. Fixed without re-freezing (the hash is still 0b9abe64f1f3ce2b): untracked the retired spellings and gated on case-folding path collisions (b3ae9e7); new pending decision [case-only-renames]. THEN InitialNodeMember (752ff41), `first X;`, SysML 8.2.2.17.1. Predicted before the code to buy ZERO corpus files, because all 11 files writing `first X;` go on to `then`, and it bought zero -- the second prediction of zero in a row to hold. `first` alone does not decide: SuccessionAsUsage and GuardedSuccession open on it too, so the lookahead reads past the whole QualifiedName and commits only on RelationshipBody's `;` or `{`. A test that asserted `first start;` was rejected -- by absence, as its own comment said -- was removed as its own commit (312fcfe) ahead of the implementation, per the tests rule. [comment-claim-checking] GAINED FOUR INSTANCES, two pre-existing (a false reachability claim that would have admitted `first` in requirement bodies had the new predicate trusted it; a wrong clause number) and two mine that the spec-conformance-reviewer caught before commit. NEW pending decisions: [wiki-receipts-vacuous] -- the `wiki receipts` check has been passing here without checking anything, and is RED when pointed at the wikis that do exist on this machine; [script-tests-skipped] -- pytest is not installed for 3.12, so no Python test suite ran in any gate. The next_step is REWRITTEN and smaller than the last one claimed: `then X;` needs an EMPTY SourceEnd and a ConnectorEnd over the existing OwnedReferenceSubsetting, not a connector-end layer."
      },
      {
        "date": "2026-09-19",
        "gate": "green",
        "summary": "Two commits closing ActionBodyItem's second alternative. Coverage 167 to 171 of 558; acceptance unchanged at 46/253 .sysml and 17/58 .kerml, AS PREDICTED -- the third zero in a row to hold; rejection corpus 106 to 109. OPENED BY CHECKING THE WIKIS: the image was mounted but SV2_SYSML_WIKI / SV2_KERML_WIKI were unset, so the receipts check was inert, and ~/.zprofile exported a misspelt SysML path (sysml-v20llm-wiki); fixed, and with the variables exported by hand the full gate was green over 1651 atoms and 189 pinned citations. THEN ActionTargetSuccessionMember in its TargetSuccession form (aabe48d), `then X;` after `first X;`, SysML 8.2.2.17.1/.17.8, with an empty SourceEndMember/SourceEnd before the `then` and a ConnectorEnd over the existing OwnedReferenceSubsetting. ORDER SETTLED FROM THE CLAUSES as the last next_step asked: the members are read in initial_node_item directly after the `first`, not as a body_elements arm, because every StructureUsageElement opens on a keyword so NonBehaviorBodyItem cannot read `then a;`; `first start; attribute x; then a;` is rejected by rule and 7.17.4's 'skipping over' (receipt 339ef468) is left to resolution. `then NAME` DOES NOT DECIDE: SendNode's optional ActionUsageDeclaration is a bare Identification, so `then s send x;` is a different production and the lookahead commits only on UsageBody's `;` or `{`. The rejection file that held `first start; then accelerate;` by absence was moved to `then action decelerate;` as its own commit (ed424c6) ahead of the production, so both commits are green. THE PREVIOUS next_step's MEASUREMENT WAS WRONG ABOUT THE PRODUCTION: it said 147 `then NAME;` lines were this; measured this session, NO corpus file writes a bare-name `then Y;` directly after `first X;`, and of 150 bare-name `then Y;` lines, 98 follow state-machine items (accept/do/entry/state) and 3 follow an action usage. The next_step is rewritten around the keyword form. The spec-conformance-reviewer found ONE BLOCKING and four advisory problems, all mine, all fixed before commit: a test cited a corpus file for `first X; then Y;` that does not contain it -- [comment-claim-checking] gains a seventh kind, a CORPUS citation, which a grep would have caught -- a feature-chain example cited from a `succession first` production, SourceEnd marked over an unreachable `[` branch (removed; `first start; [1] then a;` is grammatical and not read, so SourceEnd is unmarked), an unrecorded KerML/SysML name collision behind the ConnectorEndMember marker (new pending decision [coverage-name-collision]), and a rejection header that placed the rejection in the wrong function. Proven to bite by two mutations."
      },
      {
        "date": "2026-09-19",
        "gate": "green",
        "summary": "Made the coverage count honest, on the author's decision: resolved [coverage-name-collision]. bnf_coverage.py counts ADR-0015's 554 live grammar units instead of the inventory's 558 names, so a production the two languages state differently counts as two units and the 32 lexical terminals leave the denominator. Markers on split productions now carry `@kerml` / `@sysml`; a bare one or a scope with no live unit fails the gate with its own message. The new check, run before any marker was touched, named exactly the fifteen bare markers on split names -- thirteen became `@sysml`, RootNamespace and PackageBody carry both. 171 of 558 (30.6%) became 173 of 554 (31.2%). The reviewer's ConnectorEndMember finding, which prompted this, turned out not to be a collision under unit scoping -- it is one shared unit -- and its parser comment was corrected. Eight new tests in scripts/tests/test_bnf_coverage.py; two existing tests pinned to a fixed unit set, one reworded for the new message. The add-production skill, docs/DERIVATION.md and a stale docs/CRATES.md table updated."
      },
      {
        "date": "2026-09-21",
        "gate": "green",
        "summary": "The succession pair, resumed after the session that wrote it was killed mid-close by an out-of-memory event. SuccessionAsUsage@sysml (8.2.2.13.3), Succession@kerml and SuccessionDeclaration@kerml (8.2.5.5.3): coverage 211 to 214 of 554; acceptance 58 to 59 .sysml (training/28 Individuals and Snapshots) and 17 to 18 .kerml (Camera.kerml, predicted exactly); rejection corpus 140 to 144 files. THE SYSML PREDICTION MISSED for training/14 Action Succession Example-1 -- its two `bind` lines were never attributed -- the first miss after nine. A HANG: at_succession_as_usage looked past `snapshot` where succession_as_usage does not, spinning body_elements on `snapshot first a then b;`; the test process that exposed it was stopped but not killed, grew to 20 GB and was OOM-killed at ~10:00, taking the terminal session down. Fixed in the recogniser, and ac031cf (its own commit, ahead of this one) makes body_elements take a token whenever a pass consumes nothing, extracting the dispatch chain into body_element to stay under clippy's complexity limit; the cost of that guard is recorded as [progress-guard-masks-mismatch]. checkSuccessionSpecialization was first cited from memory as KerML 8.3.4.6.4; corrected to 8.3.4.5.4 from the wiki atom before commit. The spec-conformance-reviewer came back CLEAN: every clause against the derived units, receipts 2abd302c/b9e0de2c/2d6e6f52/86e83273 verbatim, every corpus line citation, and the one changed existing test (the unimplemented-feature list swaps `succession s;` for `succession flow ...`, required, not narrowed). Next step: the binding pair, measured at five .sysml files."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Continued from 2026-09-21 through the binding pair, AssertConstraintUsage, CalculationUsage, ConstraintUsage, RequirementUsage and the enumeration definition layer: 214 to 224 of 554 units, .sysml 59 to 90 (.kerml 18). Each production after the succession was predicted and the prediction checked: five, zero (KerML binding), seven, four, seven exact; RequirementUsage bought four of six and the enumeration first three of four. BOTH MISSES WERE THE METHOD: re-hosting a construct's body does not see what recovery swallowed after it (VehicleRequirementDerivation's `#derivation`, Verification Case Definition Example's `verification def`), and a re-host that adds a keyword hides a keywordless form (EnumerationTest's `= 60.0;`). Blanking the construct in a copy of the file now does the first; keeping the construct's own opening tokens the second. A DEFECT OF THIS RUN, FOUND AND FIXED: at_result_expression kept its own list of calculation-body items, and the four productions added before it were missing, so `first a then b;`, `bind`, `assert constraint` and `calc` in a calc/constraint body read as a result expression; a stale test hid it (3ac64b9) and at_sysml_keyword_member is now the one list (a8cb979). Every absence rejection a production made wrong was retired in its own commit ahead of it (be11cfc, 5a5a82e, df81503, 5c4ca6f). Mutation proofs caught a rejection file rejected for the wrong reason (`a + b attribute x;`, rewritten `(a + b) attribute x;`). Two prediction-era mistakes caught before they landed: a test asserting `first start;` is no calc-body item (it is), and a next step claiming `variant` is admitted only in a variation (it is admitted in every definition body; the variation rule is validateVariantMembershipOwningNamespace, 8.3.6.5). Every change reviewed by the spec-conformance-reviewer, all clean. Tooling: close-session now validates state under the project .venv (634da25)."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Second session of the day. VariantUsageMember and VariantReference; Dependency in both grammars with DependencyDeclaration; KerML RelationshipBody; ConjugatedPortTyping; and the SysML state layer in two constructs -- states, bodies, transitions, accepters and `via`, then state actions, entry transitions, effects, time and change triggers and exhibit. Units 224 to 263 of 554, .sysml 90 to 106, .kerml 18 to 19, rejection files 165 to 193, every one proved by a leaking mutation. Three absence assertions retired in their own commits first (e793207, 4fc015e). One new deviation, TransitionUsage (follow_spec, SYSML21-450). Blanking under-predicted the state layer (2 vs 4); every other prediction was exact. Slips caught before commit: implied specializations cited under the wrong constraints (blocking, found by review), a jq rewrite of deviations.json (reverted, reinserted as text), deviations.json not read before implementing six state productions (checked after; they agreed). Next: send, assign and `new`, upper bound 15 files."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Two commits, pushed: 64b05bf retired six absence rejections and the constructor rejection file; aabb2e0 read SendNode, SendNodeDeclaration, SenderReceiverPart, AcceptNode, AssignmentNode and its AssignmentTarget decomposition, the state and transition send/assign forms (marking StateActionUsage and EffectBehaviorUsage), and ConstructorExpression. Units 263 to 281; .sysml 106 to 125 (15 predicted by blanking, 4 more from AcceptNode, which the blanking left out); rejection files 193 to 200, the eight new ones each proved by a mutation that leaks. Conformance review: one blocking (a false 'reserved in both' claim for `new`), fixed; seven advisories addressed; new pending decision [keyword-table-per-language] records that the single keyword table reserves SysML-only words in KerML. Measured the next construct by blanking: connection layer, 13 files."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Two constructs, six commits, pushed. ConnectionUsage with ConnectorPart, BinaryConnectorPart and NaryConnectorPart, and ConnectorEnd's cross multiplicity in SysML (unmarked: shared, KerML multiplicity absent); .sysml 125 to 134, the nine a CORRECTED blanking predicted (the close-out's 13 had also blanked `connection def` bodies full of `end`). EndUsagePrefix, OwnedCrossFeature, UnextendedUsagePrefix and ReferenceUsage; .sysml 134 to 139, the five predicted. Units 281 to 289; rejection files 200 to 204 net (six added, two retired), each new one proved by a leaking mutation. Conformance reviews: connection 0 blocking/4 advisory; end prefix 1 blocking (`ref` taken as the kind when a later kind keyword made it a cross feature, so `end ref x part p;` was falsely rejected), fixed with tests, and 3 advisories. Next measured: analysis cases, 9 files."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "At the owner's direction: ADR-0022, deviation diagnostics and strict mode. PARSE-DEVIATION (Info) notes kept apart from errors in Parse::deviations(); Parse::is_spec_conformant(); `sv2 parse --strict`. Eight sites, each tested against a conformant twin; scripts/check_deviation_sites.py in the gate holds register and code together (sited or listed pending with a reason, fourteen pending). New register decision follow_spec_example and entry ConnectorEnd-trailing-multiplicity: 7.13.2's `mainSwitch[1]` read as the end's own multiplicity, SysML only, and flagged. Of 158 accepted corpus files, 128 are spec-conformant. Conformance review: one blocking (DefinitionElement listed pending as unimplemented while `allocation def` was read, so reported conformant), fixed and sited; three advisories fixed. Units, acceptance and rejection counts unchanged. One commit was made on a red gate by a command chain that did not stop on it; caught before push and amended green."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Nine commits: the case layer whole (Case*, AnalysisCase*, VerificationCase*, UseCase* in one CASES table; ObjectiveMember, ActorMember, RequirementVerificationMember, IncludeUseCaseUsage) and the metadata layer less `#`. Units 289 to 306, .sysml 139 to 162, rejections 204 to 210. Sites: CaseBodyItem and MetadataUsageDeclaration sited; AnnotatingElement adjudicated to need none (element kind only, ADR-0015 reach) and its register problem statement corrected. Every commit through a spec-conformance review; blocking findings fixed before commit: a comment claiming two identical declarations differed, the grammar was said to confine actors to requirement/case owners, relationship-body comment handling, and the AnnotatingElement site scope. Recovery fixes: `case` after an unread `use`, `include` likewise (guard later removed when both productions landed). Three pending decisions added."
      },
      {
        "date": "2026-09-22",
        "gate": "green",
        "summary": "Four commits: `#` prefix metadata whole in SysML (absence rejections retired first; PrefixMetadataMember/Usage/Annotation, both extension keywords threaded through every prefix and inline site, ExtendedDefinition/ExtendedUsage; EnumeratedValue sited), interfaces (Body::Interface, DefaultInterfaceEnd, InterfaceUsage's two alternatives, InterfaceEnd without ConnectorEnd's trailing-multiplicity deviation), library packages in both languages (LibraryPackage sited). Units 306 to 342, .sysml 162 to 180, rejections 210 to 218, sites 10 to 12 of 22. Every commit through a spec-conformance review: one blocking (a wrong clause number, 8.3.12.5 for 8.3.12.6), four advisories, all fixed. Mutations planted in each layer; one survived until a test reached the reader behind its recogniser. One pending decision added: shared-unit-kerml-prefix-gap."
      },
      {
        "date": "2026-09-23",
        "gate": "green",
        "summary": "Two commits: EventOccurrenceUsage over both alternatives (c92f41f), and the keywordless occurrences IndividualDefinition, IndividualUsage and PortionUsage (468bea3), with nth_opens_usage shared with at_extended_usage and simple_definition_element split out for clippy. Units 342 to 346, .sysml 180 to 200, rejections 218 to 224. Both commits through a spec-conformance review: the first clean, the second one blocking (OccurrenceDefinition cited as 8.3.9.1, it is 8.3.9.3) and one advisory (a quote that dropped the source's doubled word), both fixed before commit. Mutations: 6 of 6 caught for event after one test was sharpened; 8 of 9 for the individual layer, the ninth an equivalent mutant recorded at the recogniser. No pending decisions added or settled. Next: Message (10 files)."
      },
      {
        "date": "2026-09-23",
        "gate": "green",
        "summary": "Seven commits: Message (4d3e06e), SatisfyRequirementUsage with its absence rejections retired first (2d3c97c, 789e8bd; deviation site added), AllocationUsage (38c8529; pending decision allocation-arity added), concerns with frame and stakeholder retired first (fa3f315, 5e3d072; deviation FramedConcernUsage sited with a depth-tracked note), and optional_feature_specialization_part (893dce4; settles feature-specialization-multiplicity-gap). Units 346 to 363, .sysml 200 to 221, rejections 224 to 235, deviation sites 12 to 14 of 22. Every commit through a spec-conformance review: one blocking finding (an uncited stakeholder specialization), several advisories, all fixed before commit. Mutations planted in every layer; the survivors were three missing test cases, since added, and five equivalent mutants recorded as such. Next: FunctionOperationExpression, the `->` invocation (6 files)."
      },
      {
        "date": "2026-09-23",
        "gate": "green",
        "summary": "Sixteen commits closing the postfix expression layer and the view layer. FunctionOperationExpression with its body, function-reference and argument-list forms, BodyExpression as a base expression (deviation ExpressionBody sited, braced form only), IndexExpression, SelectExpression and CollectExpression; then viewpoints, views with render, filtered imports in both languages, and Expose. Units 363 to 395, .sysml 221 to 240, .kerml 19 to 20, rejections 235 to 239, deviation sites 14 to 15 of 22. Every commit through a spec-conformance review, whose findings (a false corpus citation, an unsupported deviation-record claim, two clause numbers, two off-by-one line citations) were fixed before commit, and every new rejection case proved by planted mutations. Pending decision expression-body-semicolon added, then settled braced-only by the author: deviation ExpressionBody narrowed with corpus evidence, ExpressionBody@sysml marked (396 units), and `attribute x = a->f;;` held rejected."
      },
      {
        "date": "2026-09-23",
        "gate": "green",
        "summary": "Fourteen commits completing the SysML action layer and the .sysml corpus. SuccessionFlowUsage; the four remaining ActionNodes, TerminateNode, WhileLoopNode, IfNode and ForLoopNode, with the shared ActionNodePrefix, ExpressionParameterMember, ActionBodyParameter(Member), IfNodeParameterMember and ForVariableDeclaration(Member), and ActionNode itself marked; and InstantiatedTypeMember's feature-chain alternative, `a.b(x)`, in both languages. Units 396 to 412, .sysml 240 to 253 of 253, rejections 240 to 253. Three spec examples found to conflict with the grammar (OnOff6's state-body terminate, 7.17.11's `if ... then {`, 7.17.12's `action step` under the one-keyword-table defect), recorded as pending decisions for the grammar-adjudicator. Every code commit through a spec-conformance review (all clean; advisories on a corpus comment, a guard-recogniser doc and a node-kind reuse, taken) and every new rejection case proved by planted mutations."
      }
    ]
  }
}
